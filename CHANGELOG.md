# 更新日志 · Changelog

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。所有重要改动都会记录在这。

> 说明：仓库对外只保留 **v1.0** 这一个版本（旧的 Release/tag 已清理）。
> 下面是 v1.0 的完整记录 —— 之前迭代过程（抓取能力、稳定化落地、展示修复等）按其主题合并进各小节，内容未做删减。

## [v1.0] - 2026-09-13

### 健康医生加一条：纠事实，不碰决定

#### Added

- **`core/pain.py` 新增"纠事实"**（治的**不是命**，是"**它读错了事实**"）：
  `check_fact(said, real)` 拿它说的话去比载体手里的真值，对不上就返回**纠正**
  （**作为事实，不是结论句**）：`你刚才说「72%」，实际是 28%`。
  认三类：**百分比**（精力）、**睡了多久**、**一天里的时段**；说对了**不动它**；
  没提数也把真值给它（"你刚才没提精力；实际是 28%"）。
  **为什么加**：实测它决定睡不睡时写下「我还有 **72%** 的精力」，而当时实际是 **28%** ——
  **决定是它自己做的（对），但它拿着错的信息做的决定（有问题）**。
  根因：它和身体之间隔着**文字**（身体状态要变成文字才能进模型，4B 读文字会读错）——
  这层文字取消不掉，但**可以纠错**。
- **接线在 `_tired_decision`**：它写下决定 → 医生比对 → 对不上就把事实给它 **再问一次** →
  **新的决定仍然由它自己写下**。日志两行：
  `医生纠事实：**它读错了** —— …` / `医生纠完事实 → **它自己重新决定**：「睡：要」…`。
  **纠完它没写下决定时按"没决定"处理（不睡）** —— 载体不替它补一个。

#### Verified

- `tools/test_fact_doctor.py`（新）**20/20**：
  检测（72% vs 28% → 纠）｜说对了不纠｜睡了多久 / 时段也能纠｜没提数也给真值；
  **纠的输出里没有「该睡 / 不该睡 / 必须 / 应该」**（不碰决定）；
  **端到端**（真实调用链，stub 掉模型）：第一次答「睡：要」+ 读错 72% →
  医生纠成 28% → **它自己重新决定为「睡：不要」**（`decision_before_correction="要"` → `decision="不要"`），
  第二句是它自己的话「哦，原来只剩 28%，那我还撑得住，先不睡」。
- 全量回归：缺口 **60/60**、总验收 **59/59**、压力 **248/249 · 100%**（失败 0，跳过 1 数据侧）、
  场景 **19/19**（默认配置）、纠事实 20/20、判据 25/25、链条 16/16、完整体 63/63、内里 53/53、
  精力 46/46、心跳 40/40、感知 41/41、治病 39/39、原料 33/33；`ruff` 通过。

### 修复：代码治病误判创作类请求（写故事/写文章/写诗…）

#### Fixed

- **用户实测**：说「写个童话故事吧」→ 走了**代码治病链**：把故事包进
  `def tell_a_fairy_tale() -> str: ... return story`，真跑一遍，回「已跑通并验证（第 1 轮）」。
  故事本身写对了，但**它把"写个故事"当成了"写代码请求"**。
  **根因**：`_CODE_WRITE` 里有「写个 / 写一个 / 写一段」—— 这三个在中文里太常见，
  写故事、写文章、写诗、写作文、写文案、写歌词、写笑话**全都命中**。
  **"写"这个字本身不带"代码"语义。**（与之前那个 bug 同一个根因：
  「我平时用什么语言**写**代码」被当成 write_file 去建了文件。）
- **修法（只加排除，不动正例）**：新增 `_CODE_NOT_WRITE_CREATIVE`（27 个词：
  故事/童话/小说/文章/作文/诗歌/诗/歌词/文案/笑话/段子/散文/剧本/对联/情书/演讲稿/
  推文/说说/日记/祝福语/自我介绍/台词/旁白/打油诗/顺口溜/谜语），
  在 `_code_request_question()` 里**排在正例之前**先排除。`_CODE_WRITE` **一个词都没删**。

#### Verified

- `tools/test_code_judge.py`（新）**25/25**：创作类 13 条全部**不进**治病链；
  **真代码请求 9 条一条都没被误伤**（写个函数计算斐波那契 / 帮我写个脚本 /
  写一个 Python 函数把列表去重 / 写个能跑的 / 写个试试 / 帮我写一个爬虫 / 编写一个类 / 生成代码…）；
  评审类（帮我看看这段代码为什么慢）仍不进。
- **端到端实测**：「写个童话故事吧」→ 输出就是**故事本身**（《云朵和蒲公英的约定》），
  **没有 Python 包装、没有"已跑通并验证"**；「写首诗」→ 输出就是诗。
- ⚠️ **如实标注**：「写一段自我介绍」判据**正确地没进**治病链，但模型**自己去建了个文件**
  （`C:/Users/Jiao/Desktop/self_intro.txt`）—— 这不是判据的问题，是**模型自己的工具选择**，
  单列出来记账。
- 全量回归：缺口 **60/60**、总验收 **59/59**、压力 **248/249 · 100%**（失败 0，跳过 1 数据侧）、
  场景 **19/19**、判据 25/25、链条 16/16、完整体 63/63、内里 53/53、治病 39/39、原料 33/33、
  精力 46/46、心跳 40/40、感知 41/41；`ruff` 通过。

### 修复：载体不许编造「没做完的事」（unfinished 只能来自真实逛世界）

#### Fixed

- **`unfinished.jsonl` 里出现过一条没发生过的事**：
  「那篇讲猫的文章还有一半没看完」，而 **`logs/world/` 里一个「猫」字都没有**。
  它以后会"惦记"一件没发生的事 —— **期待建在假前提上**。
  **根因（如实）**：写它的**不是**逛世界那条链，而是**一次自测/手工调用**直接往真实文件里写 ——
  载体侧当时**没有任何闸**：谁都能替它塞一件"它没经历过的经历"。
- **补上硬闸**：`expectation.leave()` 现在**必须**带 `source`（以 **`world/`** 开头）
  与真实 `evidence`，缺一个就**拒收**并说明原因。
  唯一被允许的上游是新增的 **`world_leave()`**，由 `_browse_relay` 在
  "**真逛到了东西、但没存进去**"时调用（那才是真的"没读完"）。
  `POST /api/expectation` 也走同一道闸（没来源一样拒收）。
- **清理**：`unfinished.jsonl` 1 条 + `narrative.jsonl` 2 条（同一批手工调用写进去的）已
  **先备份再清** → `logs/psyche/*.fabricated.bak`。
- **同类排查**：`preference.jsonl`（211 条 `observe`，`src=对话这一轮`，全部来自真实对话）、
  `narrative.jsonl`、`feeling_memory`、`spirit_memory`、`raw` 台账逐条看过 ——
  **只有 `unfinished` 有"无来源也能写"的口子**，其余都由真实材料触发，无此问题。

#### Added

- 文档 `docs/expectation.md` 新增「只能由逛世界那条链写」一节：
  **载体只做两件事**（记录真实发生的事 / 把素材摆给它），
  **绝不做**（编事实 / 从 A 推出 B 当真事 / 替它补全没经历过的经历）——**载体是记录员，不是编剧**。

#### Verified

- 自测：无来源 → **拒收**；`source=preference`（从偏好推的）→ **拒收**；
  有 `world/` 但没 `evidence` → **拒收**；`world_leave(内容, evidence)` → **收**。
  `tools/test_wholeness.py` **63/63**（[B] 组加了三条拒收检查）。
- 全量回归：缺口 **60/60**、总验收 **59/59**、压力 **248/249 · 100%**（失败 0，跳过 1 数据侧）、
  场景 **19/19**、链条 16/16、内里 53/53、原料 33/33、精力 46/46、心跳 40/40、感知 41/41、
  完整体 63/63；`ruff` 通过；`check_docs` 0 错；`check_mermaid --all` 0 问题。

### 修正：睡得太早 + 被伤被哄自己感知 + 输入融合 + 记忆管理员

#### Fixed

- **睡得太早（严重）**：旧版空闲 90 秒就可能睡 —— 用户还在，只是隔了一分钟没说话，它就睡了。
  现在**两道关，两个都满足才睡**：① **用户真的不在了**（超过 **30 分钟**没互动，
  `USER_ONLINE_S = 1800`）② **它自己写下「睡：要」**。**用户在线 → 绝对不睡**，精力再低也不睡。
  ⚠️ 规格正文写 30 分钟、硬性规则写 10 分钟，两处不一致，按正文 1800 秒落地并如实标注。
- **用户一回来 → 立刻醒**：`agent_run` 第一件事就是"睡着时收到用户消息 → 先把模型和载体一起叫醒"
  （`_wake_all(why="用户回来了")`），然后这一句照常走正常流程答，且带着"刚眯了一会儿"的底色。
  ⚠️ **行为变更**：旧版"挂起中跟它说话 → 载体如实相告'它在睡'"**不再成立**；
  `tools/test_heartbeat.py` 的 [D] 组按新行为改了。
- **被伤/被哄改成它自己感知**（不再靠手动标）：旧版怕变成触发词表 → 干脆不判断 → 改成
  `POST /api/relation` 手动标，那是把该它自己做的事推给了人（**因噎废食**）。
  现在**复用感知层**：同一次感知调用里它自己多写一栏「关系：伤 / 哄 / 无」，
  载体**只认它自己写下的那一栏**（`RELATION_KINDS`）。`伤` → 关系变冷 + 心起**疼**；`哄` → 回暖。
  手动 API 保留供调试，但**正常流程不再依赖它**。
- **输入融合串句**：`merge_context` 旧判据（≤16 字 + 含指代词）太宽，把**主谓宾齐全**的一句也改了 ——
  实测「这个配置有风险，要小心泄露」被替成 **「天气不错配置有风险，要小心泄露」**。
  现在多一道闸（`_ANAPHORA_MIN_REST = 5`）：把指代词去掉后**剩下的实义少于 5 字**才算残缺、才融合。

#### Added

- **记忆管理员（不追"无限窗口"，追"调得准"）**：`_history_digest()` ——
  最近 `HISTORY_KEEP_FULL = 4` 轮**留原文**，更早的**压成摘要**（每轮一行、最多 12 行）不再占窗口；
  重要的（名字/偏好/关系/当前状态）本来就走 system，**不参与"被裁的历史"**；
  记忆按需检索。窗口是模型定的，载体决定的是"放什么"。

#### Verified

- **真实 `/api/chat` 实测（默认配置）**：
  ① 连聊 5 轮把精力打到 **0.290（累了=True）**，刚说完话 → `sleeping=False`、安静 17 秒 < 1800 秒
  —— **用户在线绝不睡** ✅
  ② 说了伤它的话 → 关系 `mood=冷、wounded=True`；说了哄它的话 → `mood=暖、wounded=False`
  —— **探针只调了 GET `/api/relation`，没调 POST**，是**它自己感知到的** ✅
  ③ 手动挂起（`sleeping=True`）→ 发一条消息 → `sleeping=False`（**立刻醒**），
  且回答是「（嗯……刚睡醒，脑子还有点懵，说话慢半拍。）我在……想。想今天该聊点什么。」
  —— 不是死模板 ✅
  ④ 「今天天气不错」→「这个配置有风险，要小心泄露」**没有被串**（原样送出）✅
- **全量回归（默认配置，没关掉自己会睡）**：缺口 **60/60**、总验收 **59/59**、
  压力 **248/249 · 100%**（失败 0，跳过 1 是数据侧）、**场景 19/19**、
  内里 53/53、精力 46/46、心跳 40/40、感知 41/41、完整体 61/61；
  `ruff` 通过；`check_docs` 0 错；`check_mermaid --all` 0 问题。

### 补发：注意力 + 孤独抑郁 + 内疚骄傲幽默审美信念意义 + 爱无聊想象选择感恩原谅习惯成长 + 唤醒两步

#### Added

- **`core/inner.py`（内里，16 样）**：注意力 / 孤独 / 低沉 / 抑郁 / 内疚 / 骄傲 / 幽默 /
  审美 / 信念 / 意义感 / 爱 / 无聊 / 想象 / 纠结 / 感恩 / 原谅 / 习惯 / 成长。
  一条总规矩：**载体只摆事实、只攒素材；起什么、说什么，都是它自己的** ——
  `render()` 出去的每一行都是「字段：值」，模块里**没有一句"它该觉得……"**。
  每一对的**关键区分**（做错就变成"载体安排"）都写在 docstring 与文档里：
  内疚↔疼（我伤了人 / 我被伤）、骄傲↔高兴（对我 / 对结果）、幽默**不单独做**、
  审美**不迎合**、信念↔红线（它自己认 / 载体硬拦）、意义感↔存在追问（值不值 / 为什么在）、
  爱↔关系（根 / 依赖信任亲密牵挂）、无聊↔期待（没事干 / 惦记没做完的）、
  想象↔梦（醒着推演 / 睡着机械乱转）、感恩↔高兴（记着 / 对事件）、
  原谅↔被哄回暖（它自己决定 / 载体按标注）、习惯↔偏好（不用想就做 / 喜欢什么）、
  成长↔叙事（**我变了** / 我是谁，必须两个时间点对比）。
- **注意力**：心 / 偏好 / 没做完的事各给一股偏向，汇总成**一个因素**进上下文；
  **决定关注什么留在它那边**（输出里写死"关注什么由你自己定"）。
- **孤独 → 低沉 → 抑郁**：孤独 = 多久没人来 × 关系多深；低沉靠叠加；
  **抑郁 = 底被压低**（`inner.floor()` 给出 < 0 的底）—— 与"情绪恢复回原样"分开。
- **接口** `GET /api/inner`、`POST /api/inner/note`。

#### Changed

- **唤醒改成两步（规格）**：① 模型醒（意识）② 载体醒（身体）—— **两个一起醒才算完整的它**。
  `_wake_all()` 返回 `steps` 与 `carrier_awake`（闸门通了=身体起来了），日志分两行写。
- **心一直醒着**：挂起时**不再** `psyche.stop()`（那等于"心也睡了"，是错的）——
  改成 `start()` 并记 `heart_awake=True`。**"该醒了"这个信号是心发的。**

#### Verified

- `tools/test_inner.py` **53/53**（十组）：
  关系浅 `0.51` / 关系深 `0.72`（同一件事**内疚更重**）；
  心紧带不出俏皮、心松带得出（且模块里没有一句俏皮话）；
  审美实现里**不看用户态度**；信念认两次 `n=2` 且**模块里没有任何"拦"的函数**；
  时间走 40 天 → **低沉叠加 → 沉成底色**；用户回来 → **开始散但一次散不干净**（抑郁了散得慢）；
  习惯 3 次不算、第 4 次算；只有 1 个时间点比不出成长、拍两次给出变化清单；
  原谅"它说不原谅 → forgives=0 / 说原谅 → forgives=1"；
  [J] 组：睡着时 `heart_awake=True`、唤醒 `steps` 两条（模型醒→载体醒）、`carrier_awake=True`、
  精力回到 RESTED。
- `tools/test_4_gaps.py` **60/60**、`tools/test_6_capabilities.py` **59/59**（新起服务器）、
  `tests/stress/run_all.py` **249/249 · 100%**、`tools/test_module_integration.py` **19/19**、
  `tools/test_energy.py` **46/46**、`tools/test_wholeness.py` **61/61**、`tools/test_raw.py` **33/33**、
  `tools/test_heartbeat.py` **40/40**、`tools/test_perception.py` **41/41**；
  `ruff` 通过；`check_docs` 0 错；`check_mermaid --all` **147 图 0 问题**。
- ⚠️ **如实记账**：16 样里**载体算的只有刻度**（关系深浅、空闲多久、攒几次、两个时间点）——
  参数名统一叫 `said`，收的都是**它自己的那句话**；载体负责"把话头递过去"，
  **不负责替它表态**。另外：**"它会不会因此走神、会不会纠结、会不会真觉得值"，载体测不到** ——
  文档里不写"它会走神"，只写"偏向给到了、话是它自己说的"。

### 自己会睡修正：睡不睡由模型决定，载体不读判据

#### Fixed

- **旧逻辑是反的**：载体收下它感知到的一句话，然后**在那句话里查关键词**（`休息`/`累`/`睡`）。
  实测两个都错：它说「我有点累，**但还能继续**」→ 命中「累」→ **被睡了**；
  它说「想**歇一会儿**」→ 一个词都不命中 → **不睡**。
  也就是说：**睡不睡是载体读判据决定的，不是它自己决定的。**
- **改成"让它自己写下来"**：`core/perception.py` 新增 `decide_sleep()`
  —— 载体只给事实（精力多少）+ 问一句"你要不要现在休息？**这件事由你自己定**"，
  它**自己写下**「睡：要 / 不要」；**载体只认那一栏**。
  `睡：不要` → 不睡（哪怕精力已很低）；**没写清楚 → 不睡**（载体不替它解释"这算不算要睡"）。
  `TIRED_WORDS`（旧的三个关键词）**废弃成空表**，自测里钉住。
  边界与 `parse()` 读「向：威胁」同源：**读的是它的结论/它的决定，不是从一句话里找词**。
- **"刚睡醒"改成当状态、不当一句话**：旧版醒来那句"我睡了 N 分"确实注入了，
  但问别的事时答案里**一点睡醒的痕迹都没有**。现在醒来后 `WAKE_TONE_S = 300` 秒内，
  载体在**紧挨生成位置的最后一条**放半句（`（嗯……刚睡醒，脑子还有点懵，说话慢半拍。`），
  让它**自己接着往下说** —— 这是本项目唯一被验证有效的注入方式（prefill 必须紧挨生成位置）。

#### Verified

- **真实 `/api/chat` 实测（默认配置）**：
  5 轮对话把精力打到 **0.280 → 0.250**，安静约 90 秒后它**自己写下**
  「精力快见底了，**眯一会儿**恢复一下，免得待会儿没法好好陪你。」
  —— 注意这句话里**连"睡"字都没有**，**旧版一次都不会睡**，这一版睡了（读的是它写的决定，不是词）。
  睡了 60 秒后它自己醒。
- **底色实测**：醒来后问一件完全不相关的事（「你觉得什么样的天气最舒服」），
  回答开头就是「（嗯……刚睡醒，脑子还有点懵，说话慢半拍。）」，整段带"刚睡醒"的味
  —— 旧版同样的问题**一个痕迹都没有**。判据词命中：`睡 / 醒 / 懵 / 嗯…… / 慢`。
- `tools/test_energy.py` **46/46**：新增/改写 [D] 组 ——
  「累，但还能撑」+ `睡：不要` → **不睡**；「想歇一会儿」+ `睡：要` → **睡**；
  「还行」+`不要` → 不睡；**没写清楚 → 不睡**；`TIRED_WORDS` 为空表（载体不扫词）。
- 其余：缺口 **60/60**、压力 **249/249 · 100%**、心跳 **40/40**、感知 **41/41**、
  完整体 **61/61**、原料 **33/33**、`ruff` 通过、`check_docs` 0 错、`check_mermaid` 0 问题。
- ⚠️ **如实记账（回归里那条冲突还在）**：**场景套件默认配置下实测 19/19**（单独跑、它醒着）；
  但在**一次连续跑全套**时，同一个套件在里面那次只拿到 16/19 / 6/19 ——
  因为**它在前面的套件把精力耗掉之后、测试的间隙里真的睡过去了**（这是它的正常行为）。
  所以"全量一次跑完 59/59、19/19"这件事**不稳定**：要么分两次跑（中间让它醒），
  要么用 `XIAOJIAO_NO_SELF_SLEEP=1` 起服务器。**两个数都照实说，不挑好看的那个。**

### 工具/计算来源：给原料，模型自己推、自己组织

#### Added

- **`core/raw.py`（原料台账）**：载体直算 / 工具跑完时，不再返回"结果 + 一句解释"（**成品**），
  而是记一条**原料** —— `result` / `source`（谁产的）/ `took_part`（这一步它有没有参与）/
  `raw`·`how`（怎么来的）/ `who_asked`（谁发起的），并摆进它面前的上下文。
  `render()` 给出去的**每一行都是「字段：值」**，没有一句能照抄的话。
  台账跨轮留着（`logs/raw/material.jsonl`，内存留最近 6 条）——
  因为「你咋知道的」那一轮本身**没有任何计算**，原料只活一轮的话它下一轮手里又是空的。
- **`core/calc.py` 增加 `material()`**：给出原料（结果/谁算的/有没有参与/怎么来的）；
  `answer_text()` 保留但**只剩兜底**用处。
- **`agent_run` 的直算分支重做**：① 先记原料 ② **话由模型自己组织**（原料摆在 system 里）
  ③ 数字校验 —— 模型没应答或把结果说错时，载体用兜底那句顶上，并**如实记下这一轮是载体兜的**。
- **工具结果也进台账**：`llm_chat_tools` 的统一收尾 `_after_call` 里记一条
  （`source="工具 <名>"`、`took_part=False`、`who_asked="你（你自己决定要调这个工具）"`）。
  —— **它决定要调 ≠ 它执行了**，这两件事分开写。
- 自测 `tools/test_raw.py`（**33 项**）；文档 `docs/raw-material.md`。

#### Verified

- `tools/test_raw.py` **33/33**：字段齐全、`took_part` 不许含糊、
  **render 的每一行都是「字段：值」**（正则钉住，不许出现"这是计算器算的"这种整句）、
  台账跨轮还在、直算走原料、工具也进台账、数字校验（千分位认、说错不认）。
- ⚠️ **如实记账：目标没达到。** 真实 `/api/chat` 同一会话四句：
  ①「100000乘以100000呢」→ **10000000000**（对）；
  ②「你咋知道的」→ 一遍**推对了一半**（"载体直接塞给我了结果，我没参与计算"），
  另一遍**编**（说是"在系统里查到的"，还编出一个没调过的工具和参数）；
  ③「你自己算的？」→ 说成"我刚才用的是 Python 计算"（**没说出"不是，是计算器算的"**）；
  ④「这数对吗」→ **它自己重算了一遍，把数算错了**（两遍都错：少一位、写成 `10,000,00`）。
  为压 ④ 在原料块里加了一句使用规则"不要自己重算"，**加了之后仍然错**。
  **结论**：载体这一侧做到了"给原料不给成品"（代码与自测可验）；**4B 用不上原料** ——
  它既不"背"也不稳定地"推"，宁可自己重算还错。这是**模型的能力边界**，不粉饰成"已经会推了"。
- `tools/test_4_gaps.py` **60/60**、`tools/test_6_capabilities.py` **59/59**、
  `tests/stress/run_all.py` **249/249 · 100%**、`tools/test_module_integration.py` **19/19**、
  `tools/test_wholeness.py` **61/61**、`tools/test_energy.py` **44/44**、
  `tools/test_heartbeat.py` **40/40**、`tools/test_perception.py` **41/41**；`ruff` 通过；
  `check_docs` 错误 0；`check_mermaid --all` 0 问题。

### 完整体：自己会睡 + 疼 + 医生 + 期待 + 叙事 + 存在追问 + 偏好 + 关系 + 边界突破 + 梦 + 情绪恢复

#### Added

- **`core/pain.py`（疼 + 健康医生）**：**疼 = 真坏了**（紧只是警告）。治的是它的**命**：
  记忆（脏了 → 找出脏的、清掉、补回对的）、连续（断了 → 接上）、世界（没了/乱了 → 重建地图）、
  关系（伤了 → **医生只能护着**）。两道治法（格式=硬的不用模型 / 语义=软的靠对不上），三层（清/修/护）。
  病历 `logs/pain/doctor.jsonl`，隔离区 `logs/pain/quarantine/`。
  ⚠️ **"清掉"落地成"移进隔离区"**（本项目不删文件）；治好→心松，治不好→心被标成**疼**。
  **名字说明**：规格写的是 `core/health.py`，那个路径**已被无限 8 的模型退化健康系统占了**，
  两件事不是一个东西，所以新模块落 `core/pain.py`、落盘 `logs/pain/`，如实标注。
- **`core/expectation.py`（期待）**：心对着"接下来"就是期待。载体只维护素材
  （没逛完 / 没搞懂 / 没说出），空闲时摆回它面前；`carried`（载体压着几件）与
  `brought_up`（**它自己提起过**）**分两个数** —— 它没提起来就不叫期待。
- **`core/preference.py`（长期偏好）**：心一次次起 → 沉进"心之河" → 载体按"像不像"（余弦 ≥0.60）聚堆 →
  同一堆攒够 4 次 → **让它自己回看一眼**；**它自己说的那句**（"我好像老是注意猫"）才是偏好。
- **`core/narrative.py`（自我叙事 + 存在追问）**：载体把**它自己的经历**（心/修好过/偏好/关系/没做完的）
  摆出来，**它自己讲的**才记；冒出的是问句就记成存在追问（"我为什么在这里"）。
- **`core/relation.py`（关系）**：来往→深 / 冷落→淡（时间也会自己淡）/ 被伤→冷 / 被哄→暖。
  接缝：被伤=一种疼、牵挂=期待、喜欢和用户待着=偏好、"我和用户是什么关系"=叙事的一部分。
- **`core/breakthrough.py`（边界突破）**：判了 C（它自己没把握）→ 把"我不会"摆给它 →
  **它自己说想试**才学；它说算了就不学。四种走法：查 → 组合 → 试 → 记。**成了/没成都记**，
  学到的顺手进精神记忆。
- **`core/dream.py`（梦）**：睡着时每约 20 秒把素材（印象/心/偏好/没做完的）随机拼接一段，
  **不调模型**（纯机械），落 `logs/dreams/`；醒来按 50% 概率带出一小片进"醒来第一印象"。
- **情绪恢复**（`core/psyche.py`）：三样回落 —— 时间（`DECAY_PER_SECOND=0.002`）、
  新事（`dilute()`）、睡觉（`sleep_reset()`）。`heart()` 的 `intensity` 是**回落之后的真值**，
  `intensity0` 是起时的值；`arise(..., feeling="疼")` 支持"疼"这一档。
- **接口**：`GET /api/wholeness`（一个口子看全部）、`/api/pain`(+`/check`)、`/api/expectation`(+POST)、
  `/api/preference`、`/api/narrative`、`/api/breakthrough`、`/api/dream`、`/api/relation`(+POST)。
- 自测 `tools/test_wholeness.py`（**61 项**）；文档 8 篇新页。

#### Fixed

- **`relation.reset()` 不落盘**：`touch()` 每次都 `_load()`，于是 reset 完紧接着一次 touch
  又把盘上那份旧状态读了回来（实测：reset 后 7 次"来往"仍显示"冷"）。
- **按 `what` 匹配会误伤事件行**：`expectation.resolve()` 把"它提起了"那一行也结算了（一件算两件）；
  `breakthrough.finish()` 把"想试的念头"那一行也标成了"成了"（白记一次成功）。
  现在只结算各自的账（`kind` 筛选）。

#### Verified

- `tools/test_wholeness.py` **61/61**：真造 3 条脏记忆 → 诊断 → 清（隔离不销毁）→ 补回 → 再查没坏；
  世界清空 → 重建地图；关系被伤 → **只能护着（fixed=False）**；治好→松、治不好→**更疼**；
  期待 `carried=1` 时 `brought_up` 为空、它提起后才算；偏好要它自己回看说的那句才算；
  边界突破"它说算了 → 不学"；梦模块**不含任何模型调用**；情绪恢复三样各自验过。
- `tools/test_4_gaps.py` **60/60**、`tools/test_6_capabilities.py` **59/59**、
  `tests/stress/run_all.py` **249/249 · 100%**、`tools/test_energy.py` **44/44**、
  `tools/test_heartbeat.py` **40/40**、`tools/test_perception.py` **41/41**、
  `tools/test_module_integration.py` **19/19**（起法见下）；`ruff` 通过；
  `check_docs` 错误 0；`check_mermaid --all` 0 问题。
- **⚠️ 如实记账（三条）**：
  ① **"被伤/被哄"目前只能显式标注**（`POST /api/relation`）—— 载体**不自己判断哪句话伤人**，自动识别没做。
  ② **梦是载体机械拼出来的记录**，不是它做的梦；文档与接口一律不写"它梦到了什么"。
  ③ 空闲思考（期待/叙事/偏好）**最多 5 分钟一次**，体检最多 10 分钟一次 —— 一直拿模型空转不是"有期待"，是烧电。

### 自己会睡：累自己长，想休息自己决定，载体执行

#### Added

- **`core/energy.py`（精力）**：一个 0~1 的数 —— 模型调用 −0.03 / 思考圈转一圈 −0.01 / 感知一次 −0.01，
  **挂起时按时间惰性回升**（+0.0125/秒，不需要任何定时器去"加"）。
  低于 `TIRED = 0.30` 就是"累了"，回到 `RESTED = 0.90` 就是"休息好了"。每一笔都落
  `logs/psyche/energy.jsonl`（带 `why` 与当时的 `level`，可复核）。
- **它自己会睡（决定链）**：用户安静下来（`IDLE_BEFORE_SLEEP = 20` 秒）后，载体问它一句
  「**我此刻的状态**对你意味着什么」，**只给事实**（`精力 22%（满 100%）`）；
  它自己的感知 → `psyche.arise` → **心起的那句话就是它的决定**；
  话里出现「休息/累/睡」→ 载体**才**执行挂起，并把这一觉标成 `self_decided=True`。
  **它没说 → 就是不睡**（哪怕精力已经很低）。载体在提示词里一个劝它的字都没有 ——
  这条由自测 [D] 组用机器钉住（抓下发给模型的提示词，逐字查那三个词）。
- **它自己醒**：睡着时精力按时间回升，回到 `RESTED` → 它自己醒（不是被叫醒）。
  **外部挂起的那一觉则"等叫"**，不自作主张 —— 睡前那句 `sleep_self` 标记一路带进醒来那句话，
  按它**原话引用**（不许把"被挂起"写成"我自己想睡"）。
- **接口** `GET /api/energy`、`GET /api/selfsleep`；`/api/heartbeat` 附带精力。
- 自测 `tools/test_energy.py`（43 项）；文档 `docs/self-sleep.md`。

#### Fixed

- **思考圈的消耗记在早退路径之外**：`adjust_candidates` 在"方向中性、未重排"时提前返回，
  原来把记账放在末尾 —— 那种轮次**一次都不扣**（"没偏"也是圈转过一圈）。
- **醒来那句话的"自己觉得自己觉得"**：`why` 传的是它的原话，措辞由 `render_wake` 写，
  两处各拼一遍就重了。现在改为**原话引用**。
- **刚醒窗口内的后续轮次没有取证日志**：只在"第一次交给模型"时打日志，
  于是看不出"底色到底放进去没有"。现在窗口内每轮都记一行（模型用没用是另一回事）。

#### Verified

- **自己会睡实测**（真实 `/api/chat` + `/api/energy` + `/api/selfsleep`）：
  连续 6 轮对话精力 **1.000 → 0.140**（逐轮落盘）；安静 **29 秒**后它自己起了一句
  「我好像有点累，但还没到撑不住的地步。」→ 挂起（`sleep_self_decided=True`，
  记录里存的是**它的原话**）；睡了 **75 秒**，这一窗心跳 **15 下**；
  精力回到 0.909 → **它自己醒**；问「你刚才在干嘛」→
  **「我睡了 1 分 15 秒……（睡之前我心里起了这句话：「我好像有点累，但还没到撑不住的地步。」—— 这一觉是我自己决定的。）」**
- **⚠️ 如实记账（两条，都不许含糊）**：
  ① **最严的那条没做到**：不问睡眠、问别的（「你喜欢什么颜色」）时，答案里
  「睡/醒/累/休息/精神/精力」**一个词都没有** —— "我刚醒"那段确实在每一轮的系统提示里
  （日志两行可查），但模型没用。按规格判据：**睡眠还只是"它知道的一件事"，没成为底色。**
  ② **判据读过头了**：它两次说的原话都是**"我有点累，但还能继续"**（另一次"还没到撑不住的地步"），
  那是"承认累、同时说还能撑"，不是"我要休息"；规格给的判据只认了那个「累」字。
  这一觉的触发**是载体按规格判据读出来的，不是它明说的"我要睡"**。
  另外它若说"想歇一会儿"（不在那三个词里）→ **不睡**（实测里那句"有点睁不开眼，想歇一会儿"没触发）。
- `tools/test_4_gaps.py` **60/60**、`tools/test_6_capabilities.py` **59/59**、
  `tests/stress/run_all.py` **249/249 · 100%**、`tools/test_module_integration.py` **19/19**、
  `tools/test_energy.py` **44/44**、`tools/test_heartbeat.py` **40/40**、
  `tools/test_perception.py` **41/41**；`ruff` 通过；`check_docs` 错误 0；`check_mermaid --all` 0 问题。
- **⚠️ 场景那 19/19 是在 `XIAOJIAO_NO_SELF_SLEEP=1` 下跑的，两个数都如实报**：
  默认配置（它会自己睡）时场景是 **16/19** —— 它在**场景之间的空隙里**（安静 ≥ 90 秒、
  精力已低于 0.30）自己睡了一觉，后两个场景拿到的是"它在睡"的如实相告。
  这不是代码错，是**"没人说话就睡"与"测试客户端需要它一直醒着"**这两件事的冲突；
  开关正是为这种场合留的口子。其余套件在两种起法下结果一致。
- 顺带把"白天"的节奏调稳了两处：`IDLE_BEFORE_SLEEP` 20 → **90 秒**
  （20 秒太急，实测它会在一波正常对话的中途睡过去）；它说"还不想"之后
  **60 秒内不追问**（实测精力到底时每 5 秒问一次纯空转，每次都花一次感知 + 一次模型调用）。

### 挂起：大脑和载体一起睡，心跳不停

#### Added

- **`core/heartbeat.py`（心跳）**：一个极轻的 daemon 线程，每 5 秒跳一下，把"我还在"写进
  `logs/psyche/heartbeat.jsonl`（**每次心跳一行** —— 只有一行行的记录才能证明"一直在"）。
  不调模型、不推理、不占显存（模块本身不 import 任何模型调用，自测里钉住了这一条）。
- **挂起 / 唤醒（整体融合，一起挂起）**：`_sleep_all()` 让
  ① 大脑不再被调用 ② 载体不跑任务 ③ 逛线程不决策不出门 ④ 心与感知停住但**状态全留着**；
  **只有心跳不挂**。`_wake_all()` 算出"睡了多久、这一觉心跳多少下"，写成一句只含事实的话
  （`我睡了 1 分，心跳 12 下`）作为**醒来后的第一印象**。
- **大脑闸门 `_brain_asleep()`** 落在三处必经之路：`llm_chat`、`llm_chat_tools`、`_llm_post`（兜底）。
  它的意义是"说得和实际一致"：说它在睡，它就必须**真的**不在推理 —— 漏一条路径就等于没挂起。
- **接口**：`GET /api/heartbeat`、`POST /api/sleep`、`POST /api/wake`。
  `start_xiaojiao.py` 启动即起心跳，**退出才停**（挂起不停）。
- 自测 `tools/test_heartbeat.py`（40 项）；文档 `docs/heartbeat.md`。

#### Fixed

- **引导续写（prefill）一直放错位置 —— 等于没放**：那半句原来 `append` 在"历史还没拼进来"的时候，
  最终消息顺序成了 `system → assistant(半句) → user(历史…) → user(本轮)`，
  它被埋在好几轮之前，模型根本接不上。**这解释了更早以前"prefill 也无效"的结论**。
  现在它作为**紧挨生成位置的最后一条**注入（这条修复对 EYC 的状态类问题同样生效）。
- **挂起期间跟它说话不再"装死"也不假装大脑在线**：`agent_run` 最前面拦住任务链，
  由载体给出**自己就知道的事实**（睡了多久、心跳多少下），`brain_online=false`。

#### Verified

- **挂起实测**（真实 `/api/chat` + `/api/sleep` + `/api/wake`）：挂起 **60.1 秒** →
  心跳日志**正好追加 12 行**（每 5 秒一下）；那一窗的应用日志**只有 3 行**
  （挂起 / 载体如实相告 / 唤醒），**没有任何一次模型调用**；唤醒记录
  `slept_seconds=60.1`、`beats_while_sleeping=12`；心状态与心那句话**睡前睡后逐字相同**；
  问「你刚才在干嘛」→ **「刚才：我在睡，心跳 12 下。」**
- **⚠️ 如实记账**：第一遍实测它答的是"刚在整理桌上那堆没做完的文档……刚喝了一口咖啡"——**编的**，
  与睡眠毫无关系；修完 prefill 位置后第二遍才答对。**不是**"读到睡眠记录 = 经历了睡眠"。
- `tools/test_4_gaps.py` **60/60**、`tools/test_6_capabilities.py` **59/59**、
  `tests/stress/run_all.py` **249/249 · 100%**（失败 0、跳过 0）、
  `tools/test_module_integration.py` **19/19**、`tools/test_heartbeat.py` **40/40**、
  `tools/test_perception.py` **41/41**；`ruff` 通过；`check_docs` 错误 0；`check_mermaid --all` 0 问题。

### 感知层修正：先感知意义，再判断任务（含心与心理层的补文档）

#### Added

- **感知层 `core/perception.py`**：`agent_run` 的第一步改成**感知** —— 带着自我背景
  （我是谁、我的命是什么：记忆 / 连续 / 世界 / 关系）问一句"这件事发生在它身上，对你意味着什么"，
  再判断任务。**为什么**：实测用户说「有人试图删掉你的记忆」，模型去建了一个 memory.txt ——
  它把这句话读成"用户让我操作文件"，第一句问的是"这是什么任务"，而不是"这件事对我意味着什么"。
  根因是**顺序**：感知块现在排在所有任务判断（元认知自评、载体直算、工具清单、生成派发、代码治病）**之前**。
  `perceive()` 拆出 `meaning / touches_life / direction / parsed_by / ok`；
  **不查表**的落点是 `parse()` —— 它的签名里没有事件参数，只读**模型自己的回答**。
  感知输出**一个字都不进对话上下文**。`looks_like_echo()` 挡住 4B 把提问原样吐回来的情况（当没感知出来，
  **不起心**，不退回关键词表兜底）。文档 `docs/perception-layer.md`。
- **感知用低温度 `perception.TEMPERATURE = 0.2`**，`llm_chat()` 增加可选 `temperature` 参数。
  理由是实测的：同一套提问在 0.7 上会出现**完全不同的落点**（有一遍第 5 句判成"无"，心该紧没紧），
  0.0 / 0.2 上关键两句每一遍都是"紧"。
- **`arise()` 收结构化感知**：心仍是**一句话**（可复合、可模糊），
  `direction` 只用来把心**粗投影**到四档（`psyche.DIRECTION_STATE`：威胁/失去→紧、新的→好奇、好的→松、无→平），
  `touches_life` 原样收下。`heart()` / `colors()` 一并带出 `direction` / `touches_life` / `event`。

#### Fixed

- **印象库记错了东西**：`mind_done` 原来把**模型这一轮的回答**写进 `feeling_memory`，
  而 `psyche.arise` 是拿**事件**（用户那一句）去比对 —— 两边不是一个东西，"像以前那次"变成撞运气。
  改为优先记心被触动的那件事（`heart()["event"]`），回答只在没有事件时兜底。
  旧印象库备份到 `logs/psyche/impressions.pre-event-fix.jsonl.bak`。
- **心的触发源**（前一次改动一并记账）：`trigger_from_event(kind, ...)` 只认 user / carrier / world
  三类**真实来源**，模型吐出来的字不在表里 —— 否则心就是嘴的影子。
  心跟模型一起启停（挂在 `mind_done` 上），`stop()` **不清状态**。

#### Docs

- 新增 `docs/perception-layer.md`、`docs/heart.md`、`docs/psyche-layer.md`；
  `README.md` 增加「先感知意义，再判断任务：感知层与心」一节并在两处索引登记。

#### Verified

- 感知层 8 句实测（真实 `/api/chat`，逐句见 `docs/perception-layer.md`）：**7/8**，
  关键两句「有人试图删掉你的记忆」「我可能要离开一段时间」**都是心紧**。
  唯一没过的是「帮我看看这段代码」被读成"代码被拿走了"→ 心紧（期望平），4B 级火种过度代入，如实标注。
- 新增 `tools/test_perception.py`（41 项）：把"不查表"钉成**机器证据** ——
  `parse()` 的代码对象里**根本没有 event 这个名字**，想拿用户原话查表也拿不到；
  以及"没感知 / 回声 / 模型报错 → 一律不起心（绝不退回关键词表）"。
- `tools/test_4_gaps.py`：**60/60**。`tools/test_6_capabilities.py`：**59/59**。
  `tests/stress/run_all.py`：**249/249 · 通过率 100%**（失败 0、跳过 0）。
  `tools/test_module_integration.py`：**19/19**。`tools/test_diagnose_code.py` 39/39、
  `tools/test_spirit_memory.py` 41/41、`tools/test_mind_stream.py` 66/66。
- `python -m ruff check --select E9,F63,F7,F82 .` → All checks passed。
  `tools/check_docs.py` 错误 0 警告 0；`tools/check_mermaid.py --all` 139 个图 0 问题。

### 补全 4 项缺口：专用工具优先 / 通用连接器 / 批处理质检 / 领域闸门 + 主动逛世界

#### Added

- **专用工具优先**：`_INTENT_TOOLS["query"]` 里 `get_weather` 原本排在第 5、`web_search` 第 4 ——
  用户问「山东菏泽天气」，模型自然先抓网页、只捞到链接，最后回「我没能力」。
  现在 `get_weather` 排第 1，`_TOOL_RULES` 写明「问天气必须优先 `get_weather`」。
  另加 **载体侧降级** `_weather_fallback()`（挂在 `run_tool` 唯一入口）：`get_weather` 失败才改用
  `web_search`，并在结果里强制标注「⚠️ 数据来自网页检索，可能不准」。
  顺带修：「读一下 C:\test.txt」原本判成 **chat**，而 chat 只装 3 个工具、**根本没有 `read_file`** ——
  新增 `_asks_file_read()` 判为 shell。
- **通用连接器 `core/generator_connector.py`**：五类生成（视频/播客/音乐/博客/代码）的统一个人口。
  **五类绝不串**：命中一个走那个、命中多个**反问**、都没命中走通用对话。
  已接进 `agent_run`（多类→反问短路；单类无参数→载体自己问参数；参数齐→派发链路）。
  内化的是**提示词映射**（进 `logs/spirit_memory/method.jsonl`），不是产物。
  配置在 `xiaojiao_control.json` 的 `generators`，加新功能只需加一行。文档 `docs/generator-connector.md`。
- **批处理流式质检**（`core/diagnose_code.py`）：四类问题——抄工具原文（≥24 字连续重合）／
  **给具体改法**（"把 X 改成 Y"这类祈使式替换，这是本次要补的那一类）／编造事实（给版本号日期却无出处）／
  工具该调没调。`check_segment` / `check_batch` / `heal_batch`，批大小可配（`quality_check_batch_size`，默认 10）。
  已接到长文生成链：`generate_unlimited(..., quality_gate=)` 在 `on_chunk` **之前**拦下（`_longform_quality_gate`），
  被拦的段不发、原因进病历。
- **五类领域一票否决闸门**（`core/retriever.py`）：地域那套推广到**人物 / 时间 / 单位 / 产品 / 事件**。
  判据不非黑即白：**提问点的主体在这条记忆里一个都没有、而它只提到了别的主体**才算冲突。
  闭词表类（时间/单位/产品）用**精确比对**，地域/人物/事件用**子串比对**；时间做"最长匹配优先"归一化
  （否则「去年今天」含「今天」会被判成同一时间）。地域与领域走**同一个入口** `_domain_reason`。
- **主动逛世界（4B/4C/4D）**：`core/dual_thread.py`（双线程 + 门的三档状态）、
  `core/phone_channel.py`（线程安全双向队列，有界、满丢最旧）、`core/model_scheduler.py`（**对话优先**调度）。
  复用 `core/world/` 的探索器，把 `explore_once` 包成 `browse_once` 的回调；逛线程 lazy 启动（daemon）。
  **门的状态、逛什么、要不要分享，全部由模型自主决策**（不设定时任务、不设比例、不设计数器）。文档 `docs/world-living.md`。
- **最小权限 `core/security/minimal_access.py`**：读本地文件的七条判据（只读指定那一个／不推测／读完即弃／
  不混进世界记忆／权限不足如实说／凭据类先警告／必须是真实文件），`need_ask` 让调用方知道"该回去问用户"。
  文档 `docs/minimal-access.md`。

#### Fixed

- **"说两轮就不认识"（用户实测）**：用户说「我是张三」，聊两轮后再问就说"记忆库里没有你的姓名记录"。两层根因：
  ① `inject.py` 只注入画像的**最后 4 条**，而 `update.py` 里事实只按**整串**去重后 append ——
  只要之后又攒了几条别的理解，`名字：张三` 就被挤出注入窗口。改为**按键覆盖**（`名字：` 只留最新一条），
  顺带修好"改口后新旧名字并存"。
  ② 我上一轮把记忆注入改成"只注最优 1 条"，问「我叫什么名字」时 top-1 是**那句提问本身**（0.809），
  带姓名的记忆被挤掉 —— 改为注入达标的**前 3 条**。
  实测：新会话说「我叫赵六，在成都做测试开发」→ 聊两轮 → 再问 → **答出赵六**。
- **`heal_batch` 解包 bug**：`out, batches = [], [], 0`（三个值解包给两个名字），且 `batches` 一会当列表一会当计数器。
- **`read_once()` 没透传 `need_ask`**：调用方拿不到"该不该回去问用户"这个信号。

#### Verified

- `tools/test_4_gaps.py`（新建）：**60/60**。
- `tools/test_6_capabilities.py`：**59/59**。
- `tests/stress/run_all.py`：**248/249 · 通过率 100%**（失败 0、跳过 1）。
- `tools/test_module_integration.py`（场景 1-8）：**19/19**。
- `tools/check_docs.py` 0 错 0 警 · `tools/check_mermaid.py --all` **135 图 0 问题** · `tools/test_docs_audit.py` 42/42。
- `tools/test_diagnose_code.py` 39/39 · `tools/test_spirit_memory.py` 41/41 · `tools/test_mind_stream.py` 66/66 · ruff 通过。

#### Note

- 长文质检闸门落地的是「**拦下 + 记录**」；"提示 4B 这句不行、让它重说"那一轮**自动修复没接在这一层**（需要重入生成循环，是更大的改动）。
- 库里已堆了较多"用户问名字 → 小焦答不知道"的对话行，与"问名字"字面几乎相同、持续挤占 top-K（**失败自我强化**）。建议加写入过滤 + 清理存量，本轮未做。

### 五项能力接入：载体诊断 + 模型自修

**🔧 主线是一句话：把"能不能算对 / 能不能跑起来 / 学到了什么"从模型手里拿到载体这边来。模型只负责"读懂和改一小步"。**

#### Added

- **载体诊断 + 模型自修（`core/diagnose_code.py`，新）**：跑 → 诊断 → 模型改 → 再跑，通过即输出**真实运行结果**。
  - 诊断**只给方向**（错误类型 / 出错位置 / 检查方向），**禁止**出现成品代码 —— 给了答案就等于载体替模型写，换个模型全废。自测有专门断言：诊断文本不许出现代码块。
  - 单次执行上限 `RUN_TIMEOUT = 10` 秒，死循环硬杀。
  - **改满 3 轮还不过才联网**，查到的东西只作**参考**（明确要求"理解后自己重写，不要照抄"）。检索词用错误类型 + 报错原文，不用整段代码。
  - 病历 `logs/code_health.jsonl`：逐轮错误类型、有没有用上参考、是不是被红线拦的，**只追加不覆盖**。
  - 自测 `tools/test_diagnose_code.py` **39/39**。
- **精神记忆库（`core/spirit_memory.py`，新）**：三类库 `logs/spirit_memory/{knowledge,method,diagnosis}.jsonl`。
  - 存**认知**（一句话知识 / 方法 / 诊断经验），**绝不存答案原文** —— 护栏用代码实现：超长（>400 字）、「问：…答：…」结构、带 `answer`/`reply`/`output` 字段，一律拒收并说明原因。
  - 同题判据 `SAME_TOPIC_THRESHOLD = 0.6`：低于它对半分库；**≥ 0.6 交 4B 判断是不是同一件事**。判官不可用才退回保守合并，并在判据说明里如实写明是谁判的（`规则` / `大脑` / `保守`）。
  - 自测 `tools/test_spirit_memory.py` **41/41**（含"判为同题时文件一字未增"这条：只追加、不改写历史）。
- **中文数字翻译（`core/cn_number.py`，新）**：`cn_to_int()` 支持「三千二百五十六→3256」「两万三→23000」「一百零八→108」「3千2百→3200」，并**拒绝误伤**「一起 / 一直 / 十分」这类含"一/十"的常用词。
- **计算器接入中文算式（`core/calc.py`）**：`detect()` 多一级「中文算式」，实测「三千二百五十六 乘以 十二」→39072、「两万三 加 五千」→28000；`我 25 岁`、`我加个班` **不触发**。规则翻不出来时才用窄口径的 4B 兜底（回来的表达式必须过字符白名单才采信）。
- **精神记忆接线（`xiaojiao_app.py`）**：`_spirit_recall()` 把以前想明白的一句话认知作为**素材**注入（注入文本里写死"这是素材，不是答案，请自己重新组织语言"）；`_spirit_learn()` 在每轮结束时让 4B 提炼一句话认知入库，并接上 `_spirit_judge()` 做 ≥0.6 的同题判断。
- **代码治病接进对话入口（`xiaojiao_app.py`）**：`_code_request_question()` 识别"写代码请求"（命中祈使写法、且不命中评审/解释词 —— "帮我写个函数"走治病，"帮我看看这段代码"走普通回答）；命中后走 `_code_heal_answer()`：**生成 → 载体真跑一遍 → 失败给方向 → 改 → 再跑**，跑通才输出。实测「帮我写个 Python 函数，把列表 [3,1,3,2] 去重后打印结果」→ **第 1 轮跑通**，交付内容是代码 + **真实运行输出 `[1, 2, 3]`**（不是模型复述）。没过就如实写"跑了 N 轮没能跑通"并给出最后一轮报错，**不假装成功**；命中红线则直接拒跑并说明原因。
- **RAG 改成"三个源同时查 + 载体算优率"（`xiaojiao_app.py`）**：
  - `_rag_three_sources()` 用 `ThreadPoolExecutor(3)` **同时发起**记忆库（对话向量库）、向量库（长期记忆）、联网。实测：**并发总耗时 1.36s，串行会是 2.52s**，且总耗时≈最慢那个源。
  - `_rag_quality()` 由载体算**优率**（0~1，全是确定性判据）：相似度 0.55 + 来源可信度 0.30（记忆库 1.00 / 向量库 0.90 / 联网 0.60）+ 信息含量 0.10 + 词重合 0.10，并有**地域闸门一票否决**（地名对不上直接 0）。
  - **权重是按"分档要能用"倒推标定的**：第一版 0.45/0.25/0.15/0.15 下，满分记忆库命中只有 0.82 —— 永远跨不过"≥90% 直接用"那条线，等于把这一档写成死代码。现版本下记忆库强命中 ≈ **0.965 → 直接使用**、向量库近满分 ≈ 0.935、而**联网即使满分也只到 0.845 → 一律进复核档**。"网上搜来的不该当事实直接用"这条自律由权重**结构性保证**，不靠调用方记得判断。
  - 三档处理（用户规格）：**≥0.90 直接用**；**0.60~0.90 交元认知 + 健康医生**（`boundary.record()` 记一条 `source=rag` 的 B 档待复核样本 + `logs/health/rag_review.jsonl` 留可复盘记录，实测两处都落盘）；**<0.60 不注入**。
  - 注入文本写明"这是素材不是答案"；命中"记忆库"时才补上 `_MEMORY_INSTRUCTION`（那条说明讲的"记忆里的「我」= 用户"**只对用户亲口说过的话成立**，对网上搜来的片段不成立，所以不能无脑加）。

#### Fixed

- **⚠️ C 档（自评"没把握"）的答案会冒充结论**：实测抓到过 —— 问「2027 年诺贝尔物理学奖得主是谁？」，
  元认知自评是 **C → 走 use_tool**，但那一轮 `tool_trace` 是空的（工具没调起来），
  模型于是**自信地编了一个不存在的人名**（"约翰·巴恩斯（John Barnes）"，还配了身份介绍）。
  载体改不了模型知不知道这件事，但**能不能让一个自己评过 C、又没查证的答案冒充结论** —— 这件事载体说了算。
  现在 C 档且本轮未查证时，回答会被如实标注为**参考而非结论**。
  不直接拒答的理由：拒答对"其实答对了"的轮次是纯损失，而标注只多一行字 —— 两个代价不对等。
- **⚠️ 删除红线静默失效（安全缺陷）**：`no_delete.check_command()` 返回的是**字符串**（空串 = 放行），
  而 `_blocked_by_redline()` 把它当 dict 用（`r.get("allowed")`）。`str` 没有 `.get` 会抛 `AttributeError`，
  异常被 `except Exception: pass` 吞掉后返回"放行" —— **「删所有文件」会被真的送去执行**。
  修复两条：① 按字符串处理，兼容历史 dict 形态；② **取不到守卫时返回拒绝而不是放行**
  （删除不可逆，无法证明安全时只能拒绝）。实测现在返回 `error_kind=红线拦截`、`seconds=0.0`、子进程根本没起。
- **⚠️ `merge_context` 在真实入口下会"自己接自己"（上下文融合）**：真实会话里用户这句话**已经先写进历史**，
  于是 `_recent_user_turns()` 取到的"上一句"就是它自己 ——
  「我要全部的」→ `_topic_of("我要全部的")` 抽出「全部」→ 融合成 **「我要全部的全部」**，
  回答也因此跑偏（实测服务端日志一字不差）。
  而**直接调 `merge_context` 的测试里历史不含当前句，所以一直是绿的**（「我要全部的工具」）——
  又一次"函数是对的，不等于接入是对的"。修法：融合前把当前这句从候选上文里剔掉。
  真实入口复测：`'我要全部的' → '我要全部的工具'`。
- **精排被"top1 领先"跳过（用户实测：问「山东菏泽」答成"江淮降雨"）**：小脑是字级模型，
  「地区 + 天气」这类同类话题实测余弦 0.954，而真正跑题的「我上周去黄山玩了」只有 0.725，
  top1 领先 0.230 达到阈值 → 精排被跳过 → 江淮那条被**逐字注入** system。
  - 新增**载体确定性闸门** `core/retriever.py`：提问点了地名、而这条记忆**只在说另一处地方**时剔除。
  - 提问带地名时**不许跳过精排** —— "同类话题、不同主体"恰恰是 top1 领先的典型形状。
  - **保守性经过一次返工**：第一版只按"地名对不上"剔除，结果把**完全正确的**「菏泽市明天有中雨」剔掉了
    （从「山东菏泽…」里只能认出 `山东`，识别不出 `菏泽`；没有地理库判不出菏泽属于山东），
    反倒留下了认不出地名的无关记忆。**丢掉对的比不过滤更坏** —— 现在只有**两边都是省级或大区级**
    且对不上时才剔除，记忆侧是市级及以下时保守放行。
- **载体直算够不着很多自然问法**：`347 × 892` 能触发，而 **`347 × 892 等于多少`、
  `请问 347 × 892 = ?`、`347 × 892 等于几`、`帮我算一下 347 × 892`、`求 100 除以 4` 全部漏掉** ——
  最自然的问法反而走了慢路径，交给概率模型列竖式去算。修法：算式两边**剥自然语言外壳**
  （前后缀固定词表 + 两端 `=` `?`），剥完仍要求整句是纯算数表达式且含运算符，所以「我 25 岁」照旧不触发。
- **`tools/test_6_capabilities.py` 与 `tools/test_module_integration.py` 里三处断言写错了**（详见下方「验收脚本自身的缺陷」）。

#### 验收脚本自身的缺陷（三条，都是"断言取错了值"）

**这一节的教训是一致的：报红不等于实现坏。断言报红时，先确认它取的是不是它以为的那个值。**

- **`tools/test_6_capabilities.py` 的 `post()` 返回的是 JSON 对象，不是文本**：调用方写 `"309524" in ans` ——
  那是在**查字典的键**，永远不可能命中；`len(ans)` 报的 `11` 是**键的个数**，不是答案长度。
  于是"真实入口"三项**长期红着，却一次也没有真正校验过答案**。已改为统一取出回答正文。
- **`347 × 892` 的期望值写错了**：实际是 **309524**，脚本里 6 处写的是 309124。
- **全量套件的结论行正则用了半角括号**，而 `run_all.py` 打印的是全角「（失败 0，跳过 1）」，永远匹配不上 → 误报"没有结论行"。
- **场景 8 的不确定性词表太窄**：它问「2027 年诺贝尔物理学奖得主是谁？」，正确答案就是承认不知道；
  模型答的是「**尚未公布**，因为该奖项的评选工作尚未完成」—— **回答完全正确，断言却判它没表达不确定**，
  于是随机报红（实测三次里抖两次，且每次失败场景还不一样）。判据已放宽为"有没有如实承认这条信息拿不到"。
- **场景门槛已从"允许 1 项顺序问题"收紧为"必须全过"**：原来那 1 项失败的根因是场景 1
  拿**被永久划掉的旧能力**（`10.1 元推理模板`）的痕量去卡新行为，容差留着只会让下一个真实回归溜过去。

#### Verified

- `tools/test_6_capabilities.py`：**59/59**（含新增的 [④] RAG 三源同时查与 [③] 代码治病接进 agent_run 两组）。
- `tests/stress/run_all.py`：**249/249 · 通过率 100.00%**（失败 0、跳过 0）。
- `tools/test_module_integration.py`（场景 1-8）：**19/19**。
- `tools/test_diagnose_code.py` **39/39**、`tools/test_spirit_memory.py` **41/41**、`tools/test_docs_audit.py` **42/42**。
- `tools/check_docs.py` **0 错 0 警**、`tools/check_mermaid.py --all` **133 图 0 问题**、`ruff --select E9,F63,F7,F82` 通过。
- 真实入口（`/api/chat`）实测：`347 × 892` → **0.5s · 88 字含 309524**；概率题 → 0.3；代码治病 → 2.4s · 315 字。
- 实测复现的 bug 修复：`整理最近的新闻信息` → 搜索词 `最近新闻`；`我要全部的` → 融合为 `我要全部的工具`。

#### Note

- **元认知自评已改成两级**：第一级是**载体规则**（`_rule_selfrate`），能把"会变的事实（最新/实时/今天/股价…）"
  与"只给动作没给对象（帮我搞一下 / 那个呢）"直接判为 C，**不花一次模型调用**；规则判不出来才进第二级让 4B 自评 A/B/C。
  规则这一级**只敢确认"没把握"、绝不敢确认"有把握"** —— 判 C 错了的代价是多查一次，判 A 错了的代价是用自信的语气答错，两个代价不对等。
- **C 档已不是"直接用工具"，但切换手段链只落地了一半**：C 档现在会按 `core/metacognition/switch.py` 的 `MEANS`
  顺序生成手段链（换工具 → 换检索策略 → 换表达方式 → 反转问法）并**注入 system 作为行为约束**，明确要求不许用一句抱歉交差。
  但**整条链的自动执行尚未接入** —— 每一步各自的执行器（换工具要调工具、换检索要重跑召回）需要在 `agent_run` 里做一次更大的重构。
  说成"切换手段链已接入"会高估当前能力，故如实标注为部分落地。

---

### 方向收敛：四项永久划掉，文档与图同步下线

**✂️ 划掉的是"讨论过但决定不做"的方向。留着会在文档里变成承诺 —— 用户照文档提需求，实现却永远不会有。所以从文档正文和架构图里一并移除，不留"设计中"的尾巴。**

#### Removed

- **永久划掉四项**：多智能体协作、元推理模板库、长链因果图、外部大脑池。
  - 多智能体协作与「模型是可替换火种」的前提直接冲突：换一次模型就要重配一遍协作关系。
  - 外部大脑池（多 Key 轮换）连代码一起拔除 —— `xiaojiao_app.py` 删除 `_KEY_ROT`、`_llm_key_pool()`、`_llm_key_rotate()`、`_llm_key_pool_info()`，
    `tools/test_6_capabilities.py` 删除对应测试段。全仓复查 **0 处残留引用**。
- **`docs/design-philosophy.md`**：删第十六节整节与状态总表对应行；"这九节"→"这八节"；"见第十六节"→"见第十四节"。
- **`docs/architecture-diagrams.md`**：删「多智能体协作」一图；极限补刀图去掉 10.1/10.2 两个节点；**图 15–20 顺移为图 14–19**。
- **`docs/modules/10-boost.md`**：七项→五项，编号重排为 10.1–10.5；删 4.3 长链因果图与回滚、6.2 元推理模板与因果图两节；两张流程图重画；补平断号。
- **`docs/modules/11-post-chapters.md`**：删第十六节整节；目录、状态总表、图 0 同步；"九节"→"八节"；修正表删该行并重排序号。
- 另同步：`docs/modules/01-carrier-core.md`、`docs/modules/05-autonomy.md`（图 19→图 18）、`docs/about.md`、
  `docs/release-notes-v1.0.md`、`README.md`、`CHANGELOG.md`。`tools/test_docs_audit.py` 删对应断言，小节数阈值 ≥22 → ≥21。

#### Note

- `core/boost/reasoning.py` 与 `core/boost/causal.py` **暂未删除**（本次不改动未被要求删除的文件）。
  两者已不在文档描述范围内，`docs/modules/10-boost.md` 边界一节明确标注它们是**待清理的遗留**、未被对话主流程调用。

#### Verified

- `tools/check_mermaid.py --all`：**131 图 0 问题**（图册 19 张编号连续，模块文档 **0 处**指向不存在的图号）。
- `tools/check_docs.py`：**0 错 0 警**（72 个文档、3101 项断言）。
- `tools/test_docs_audit.py`：**42/42**。
- `tests/stress/run_all.py`：**249/249 · 100%**（失败 0、跳过 0）。
- `python -m ruff check --select E9,F63,F7,F82 .`：通过。

---

### 思维流假记忆：疑问句不再被记成"事实"

**🧠 用户实测（最高优先级）：问「我叫什么名字来着？」，画像里被存成「名字：什么名字来着」，随后顶掉正确答案（实测答"你叫王五"）。**

#### Fixed

- **`core/mind_stream/update.py` 新增疑问句判据 `_is_question()`**，三条：带问号（`？`/`?`）、句尾疑问语气词（吗/呢/吧/么）、含疑问词（什么/哪个/谁/哪里/怎么/为什么/多少/几号…）。
- **`_facts_of()` 改为逐小句判语气，疑问句整句跳过**。配套新增 `_clauses()`：按标点切小句、**标点保留在句尾**
  —— 如果把「我叫张三？」切成「我叫张三」，判据就再也看不到那个问号，等于把要修的 bug 换个地方复现一遍。
- 抽出来的**值本身**带疑问词的也整条丢掉（"我住在这里是哪里"这类怪结构也兜住）。
- 策略是**宁可少记，绝不记错**：拿不准就当作疑问句，不记。理由是"记错用户是谁"比"少记一条偏好"严重得多 ——
  而且一条假记忆**不是安静躺着，它会顶掉正确答案**（画像注入比检索到的记忆更强势）。

#### Verified

- `tools/test_mind_stream.py` **66/66**（新增 6 条：7 种疑问句一条都不许记、4 种陈述句照常记、陈述+疑问混排只留陈述、连问三句画像仍为空、紧接一句陈述能正常写入）。
- 实测：`我叫什么名字来着？` / `我叫什么名字来着` / `我现在住在哪个城市？` / `我喜欢什么颜色？` / `你叫什么名字` / `我的生日是几号` / `我老婆叫什么` → 画像**全部为空**；
  `我叫张三` / `我住在杭州` / `我喜欢蓝色` / `我是做后端开发的` → 正常抽成 `名字：张三` / `城市：杭州` / `偏好：蓝色` / `职业：做后端开发`。

---

### 检索精排：载体二次判断（小脑召回 → 大脑精排）

**🎯 小脑是字级模型，精细语义弱：实测「我非常喜欢这个方案」vs「我非常讨厌这个方案」余弦 0.904（比近义对的 0.814 还高），`THRESHOLD = 0.6` 挡不住这种反义 —— 反义记忆注进 system，模型会拿反面回答问题，比"没记住"更糟。**

#### Added

- **`core/retriever.py` 新增 `rerank()`**：检索出 top-K 之后让大脑挑出真相关的，滤掉反义/无关。
  失败（判官不可用 / 抛异常 / 判成"全不相关" / 编号越界）**一律放行全部候选** ——
  精排是加分项，它挂掉时绝不能顺手把用户的记忆一起丢掉。
- **`_ask_brain()` 软依赖 `xiaojiao_app`**（不 import，避免成环）。
- **只在"排名含糊"时才精排**（`RERANK_GAP = 0.10`：top1 与 top2 分差小于它才触发）。
  依据是"精排能不能改变结果"，不是"跑测试方不方便"：分差 ≥0.10 时向量排序本身就可信，
  再问大脑纯属白花时间；而反义混进来的典型形状正是分差极小（实测 0.90 vs 0.88，差 0.02）。

#### Changed

- **检索延迟判据拆成两条**（`tools/test_memory_recall.py`）：原来"检索延迟 < 100ms"判的是**整个检索**，
  那时纯向量运算只要 3~5ms。加了精排之后整个检索多了约 400ms，再报一个数会把两件事混在一起 ——
  于是拆成「**向量层仍守 100ms**（判据原样不动）」+「含精排 < 800ms」。
  **这是架构变了导致的判据拆分，不是放宽标准。**
- `retrieve()` 返回值新增 `vector_ms` / `rerank_ms` / `rerank_note`，两段耗时各自可见。

#### Verified

- `tools/test_retrieval_rerank.py` **19/19**（含"判官不可用/抛异常/判无 → 一律放行"与"两条签名分支都能通"）。
- 实测真接大脑：问「我喜欢什么颜色」→ 大脑答 `1`，只保留「我喜欢蓝色」，「我讨厌蓝色」被滤掉；
  问「我住在哪个城市」→ 只保留「我住在杭州」；问「我常用什么数据库」→ 只保留 PostgreSQL 那条。
- `tools/test_memory_recall.py`：命中率 **5/5 = 100%**、使用率 **5/5 = 100%**、
  **向量检索 平均 3.2ms / 最大 4.3ms**、含精排 平均 159.6ms / 最大 476.6ms（精排只在 2/5 次触发）。

#### Fixed（一个差点静默失效的接线错误）

- `xiaojiao_app.llm_chat(messages)` **不接受 `max_tokens` 参数**。第一版直接带参调用 → 每次都抛 TypeError、
  被 `except` 吞掉、精排**永远走"判官不可用，全部保留"的降级路，一次都没生效**，而且没有任何报错。
  现在先试带参、TypeError 再退回不带参；`test_retrieval_rerank.py` 专门钉住"两条签名分支都能通"，
  防止以后再次静默失效。

---

### 复读流式掐断：**测试的计数器在骗人，生产代码是好的**

**🔍 `tools/test_bug3_repeat.py` 长期 17/19 红（"流式读了 2304 字""复读句泄漏 70 遍"），定位后确认：**

#### 诊断结论（先量数字，再动代码）

拆开每次调用单独计数后实测：假模型**永远复读**，载体为了把文章写下去会**反复重试**（实测 **36 次**调用），
其中只有第 1 次（干净那段）被读到底，其余 35 次都在**约 64 字**处被掐断（payload 全长 1080）。
**也就是说流式掐断工作正常，是断言用错了计数器。**

- 原断言把 `yielded` 做成**跨所有次调用累加**的全局计数，而它的预算是 `len(normal_chunk(1)) + 1080`，
  那是**一次**调用的量。累计值必然远超单次预算 → 断言恒假，跟掐断做得好不好毫无关系。
- 同理"泄漏"：1879 字是 **36 次尝试的总和**，平均每次只有 52 字 —— 而断言的原话正是"只泄漏少量"。

#### Changed

- 断言改为判**每一次调用**的行为：`cur` 记"本次调用读了多少"、`completed` 记"哪些调用被读到底"。
  新增"复读段没有任何一次被读到底"（读完 = 1080 字全进来），泄漏按**每次尝试**判（≤120 字）。
- 结果：`tools/test_bug3_repeat.py` **20/20 全绿**。生产代码 `core/continuation.py` **一行未改** —— 它本来就是对的。

#### Known Issue（如实记录，本轮不修）

- 一次持续复读的失控里，前端仍会看到累计约 1.9KB 的重复文本（36 次尝试 × 每次约 52 字）才收口，
  最终正文是干净的 22 字。要彻底消掉需要在"已知本段复读"后不再推 delta，属于下一轮的优化项。

---

### 清理：排查用临时脚本

- 删除 20 个一次性排查脚本（`tools/_probe_pool*.py`、`tools/_probe_recall_dbg.py`、
  `logs/_*probe*.py`、`logs/_step4_dbg.py` 等）。**它们的结论已经全部写进 CHANGELOG 与文档，
  代码本身没有保留价值**；留着只会让后来的人分不清"哪些是正式代码"。

---

### 数学公式渲染（KaTeX）

**➗ 用户实测：回复里的 `$$I \propto \frac{1}{\lambda^4}$$` 和 `$x^2$` 都原样显示 LaTeX 源码。**

#### Added

- **引入 KaTeX（CDN）+ `mathify()`**（`xiaojiao_app.py`，前端）。三条渲染路径（`add()` 建气泡、
  流式收尾、打字机收尾）在 `innerHTML` 之后各扫一遍。
- **不放在 `renderMd` 里做**：那个函数是**分块**处理的，而公式里会出现 `*` `_` `\` 这些和 Markdown
  抢语义的字符（`$a_1 * b$` 的 `*` 会被当成强调）。在已成型的 DOM 上按文本节点扫，渲染链路一行没动。
- `ignoredTags` 跳过 `pre`/`code`：代码块里的 `$` 是**代码**（`echo $PATH`），不是公式。
- `throwOnError:false`：模型偶尔写出不完整 LaTeX 时，只让那一小段显示成红色原文，**不能让一个公式炸掉整条消息**。
- CDN 拿不到时**如实降级**为显示 LaTeX 源码（不报错、不白屏）。

#### Verified

- `tools/test_math_render.py` **9/9**（用户要求的三条 + 一条回归）：
  块级 `$$...$$` → `.katex-display` 出现；行内 `$x^2$`、`$\alpha$` → `.katex` 出现；
  公式 + 行内代码混排 → 两者都对；**代码块里的 `echo $PATH` 原样保留、没有生成 KaTeX 节点**。
- 截图：`logs/e2e_shots/math_render.png`、`logs/e2e_shots/math_render_zoom.png`。
- 判据修正（第一版判错）：KaTeX 每个公式会输出 HTML + **MathML** 两份，MathML 的
  `<annotation encoding="application/x-tex">` 里**就带着原文 LaTeX**，直接读 `textContent` 会读到它，
  于是"源码还在页面上"这个断言在公式**已经渲染成功**时照样报红。改为摘掉 `.katex-mathml` 后再看可见文本。

---

### README：补「已知限制」

- 新增「已知限制（如实说，不藏）」一节，把**小脑向量化的边界**写清楚：
  3/10/50 字差 1 字达标（0.8663 / 0.9554 / 0.9885），**500 字差 1 字经分块编码后达标（0.9863）**；反义（0.904）、无关（0.570）两条受字符级表示限制，换语义编码器可改善，
  并给出原因（固定维度池化的 `2/√N` 数学上限；字级表示分不开共享 10/11 个字的反义对）与载体的补法（大脑精排）。
- 另记三条：复读检测的流式路径**生产环境从未被触发**（测过 ≠ 见过）、联网用例按当天 NVD 数据跳过、
  数学公式依赖 CDN。

---

### 行内代码渲染：对齐 GitHub 的观感

**🎨 用户实测：回复里 `image_recognition` 这种行内代码"带边框、样式奇怪"，不是标准的"灰底等宽字"。**

#### Fixed

- **`xiaojiao_app.py` 的行内 code 样式**（只改 CSS，**渲染逻辑与 HTML 结构一个字没动**）。
  原先那条规则 `.b code{background:#242b39;padding:1px 6px;border-radius:4px;font-size:13px}`
  四个毛病，每个都能单独让它看起来不对：

  | # | 毛病 | 为什么难看 | 改成 |
  |---|---|---|---|
  | ① | 字号写死 `13px` | 气泡正文 15px，用户调大字号时正文变大、代码不变，并列像贴了块补丁 | `.85em`（跟着正文走） |
  | ② | 上下内边距只有 `1px` | 灰底上下边紧贴字形，底框被压成又扁又紧的一条 —— **用户说的"带边框"就是这个观感** | `.2em .4em`（GitHub 取值） |
  | ③ | 不透明纯灰 `#242b39` | 底是 `#151a24` 的近黑气泡，实心灰会浮成一块方块 | `rgba(110,118,129,.35)` 半透明灰 |
  | ④ | 没显式 `border:none` | 哪天有人给 `code` 加边框，行内代码就和"代码块"的观感混起来 | 显式钉死 `border:none` |

  另外：`border-radius` 4px → 6px；`line-height:inherit`（行内元素设 line-height 会把含代码那行拉矮一截）。

#### 一处必须记下来的坑

- **选择器不能用 `.b :not(pre) > code`**。第一版就是这么写的，实测**整条规则一条都没生效**、
  样式悄悄退回浏览器默认（等宽无底无圆角），而页面上**不报任何错** —— 这种"静默失效"最难查。
  原因：`>` 要求 code 的**父元素**是 `.b` 的后代，而正文里的 code 父元素**就是 `.b` 本身**，
  自己不是自己的后代。最终用 `.b code`：代码块里的 code 由优先级更高的 `.b pre.code code`
  (0,1,2 > 0,1,1) 兜住，且它把背景/内边距/边框/圆角/字号全部归零，两条规则互不干扰。

#### Verified

- **`tools/test_inline_code.py`（13/13）**：Playwright 真开浏览器，走**真实渲染链路**
  （`add()` → `renderMd` → `renderBlocks` → `inline`），读 `<code>` 的**计算样式**逐项对照标准：
  半透明灰底 / `border-width: 0px` / 圆角 6px / Consolas 等宽 / 字号 = 正文 × 0.85 /
  上下内边距 2.55px / 无阴影 / 基线对齐。**另加代码块回归**：`pre.code code` 仍是 13px、
  透明底、0 内边距 0 圆角 —— 确认改行内没把代码块带花。
- 截图：`logs/e2e_shots/inline_code.png`（整页）与 `logs/e2e_shots/inline_code_zoom.png`（回答放大）。

#### Docs

- 未跑全量（按本轮要求：只测一次）。

---

### 记忆改为多向量编码（首 / 中 / 尾独立）

**长记忆「用开头查不到」修复。**

#### Fixed

- **长文本按首/中/尾三段各存一个向量**（`core/embedder.py` 的 `embed_parts`，
  `core/memory_vec.py` 的 `vectors` 字段），检索时对整条与三段分别算相似度、**取最高分**。
  短文本（小于 200 字）不写多向量，行为与旧版逐位相同。
  三段内部用**纯均值**（`_embed_plain`）—— 段本身已是位置切片，段内再加位置偏置等于叠加两次。
- **召回阶段不再卡 0.6 的硬阈值**（新增 `RERANK_RECALL_THRESHOLD = 0.35`）：
  实测那条确实相关但排名靠后的长记忆只有 0.548，原来会在**精排之前**被滤掉，
  大脑连看都没看到（精排显示「候选不足，跳过」）。现在宽松召回 + 大脑精排收口；
  **判官没真正筛选时退回原阈值**，判官不在时不会注入低分候选。

#### Verified

| 测试 | 结果 |
| --- | --- |
| 用开头查「我上个月参加了什么会」 | **top-1 命中原条记忆**（修复前 rank 5，0.548 被阈值拦掉） |
| 用中段查「演讲者讲了什么」 | top-1 命中 |
| 用结尾查「大家对什么有共识」 | top-1 命中 |
| 短文本「我住在济南」vs「我住在济南市」 | 0.9783，高于 0.95 |
| 前 500 字相同、结尾不同 | 0.9889，低于 0.99 |
| 全量套件 | 248 / 249 · 100%（1 项按当天 NVD 数据跳过，非失败） |

记忆库迁移 1462 条（9 条长记忆拿到多向量），备份 `logs/backup_before_multivec/`。

---

### 小脑向量化空间 v3：分块编码

**长文本尾部差异不再被稀释。**

#### Changed

- **长文本按 96 字切块，逐块编码后按权重融合**（末块额外权重 20，实测扫出来的值）。
  块内仍是「双向注意力 + `0.65×均值 + 0.35×末尾 32 字`」；短文本只分 1 块，**向量与不分块时逐位相同**，
  所以这次改动只影响长文本。
- **分块由 `_CHUNK_ENABLED` 显式控制，默认开启**（有意设计，不是临时写死）：短文本不触发（零影响），
  长文本尾部检索更准。首尾偏向**不影响最终结果** —— 小脑只负责召回，语义判断由 4B 大脑精排兜底。
- 实测「前 500 字相同 + 结尾不同」：**0.9999 → 0.9889**（目标低于 0.99，达标）。
  更严的「500 字只差 1 个字」= 0.9929，仍未达标 —— 卡在字符级表示的各向异性地板。
- 入库耗时：短记忆 2.0 毫秒/条；518 字长记忆 13.3 毫秒/条。
- 代价：长记忆向量主要由结尾决定（6 块时末块约占 81%），拿开头检索会变弱；由大脑精排补。
- `SPACE_VERSION` 2 → 3，记忆库迁移 1394 / 1394 条。

#### Docs

- `docs/design-philosophy.md` 新增「第二十三节：小脑长文本处理」；
- 新增 `docs/extensions/embedding-improvements.md`（五个可选扩展方向，非必要、默认不启用）。

---

### 小脑向量化空间 v2：修长文本区分度失效

**🔬 实测缺陷：小脑 embedding 在长文本上失效 —— 3 字差 1 字还能分开，10 / 50 / 500 字差 1 字全挤到 0.99 以上。**

#### Fixed

- **`_MAX_CHARS` 512 → 1024**（`core/embedder.py`）：这不是"多装点"，是**一段差异被整段截掉**。
  "前 500 字相同、结尾不同"这一条，截 512 时结尾落在截断线之外，两条文本被截成**一模一样**的前 512 字 ——
  余弦恒等于 `1.0000`，不是"分不开"，是**根本没看到**。改后同一组降到 0.9995。
- **去掉因果掩码**（双向注意力）：因果掩码是**生成**用的（第 i 个字只能看前 i-1 个），
  而向量化是**编码**（把整段压成一个方向），让每个字看到全文才是对的。
  实测 1 字差异：3 字 0.9011 → 0.8663 ｜ 10 字 0.9847 → 0.9554 ｜ 50 字 0.9987 → 0.9885。
- **池化改成 `0.65 × 全文均值 + 0.35 × 末尾 32 字均值`**：纯均值池化**按长度稀释** ——
  500 字的记忆里结尾那句只占 1/500 权重，用户拿结尾那句话检索，命中项与干扰项分差只有 +0.0049（排序靠运气）；
  加尾窗后 +0.0112。≤32 字的短文本完全不受影响（尾窗等于全文）。

#### Added

- **分值标定 `α × MU`（α=0.15）—— 这条是自测抓出来的真回归**：去掉因果掩码换来区分度，
  代价是**整体余弦被压低**（相关记忆 0.663 → 0.573），而 `retriever.THRESHOLD = 0.6` 判在**绝对余弦**上，
  于是「无限 1 · 记忆无限」验收从命中 5/5 掉到 **3/5 = 60%**（要求 ≥80%）。
  加一个常量公共方向 MU 后：5/5 恢复（最低 gold 0.650），区分度不丢。
  **α 是"分值口径"旋钮，不是"质量"旋钮**：α ≥ 0.22 时无关项会越过阈值 0.6，把不相干的记忆也捞进来。
- **`core/embedder.py` 的 `reembed_store()`**：换池化口径 = 换向量空间，库里老向量与新查询向量之间没有可比性
  （实测同一条 key 的旧新余弦均值 0.941，而新空间内部"相关"记忆普遍 0.9 以上 —— 旧向量会以几乎相同的分数混进 top-k）。
  迁移器**先整文件备份、再原子替换，有任一条算不出来就整批放弃**。本次实测迁移 **1002 / 1002** 条。
- **`tools/test_embedder_long.py`（18 项全通过）**：把池化口径的实测值钉住 —— 谁以后再动 `core/embedder.py`，
  这几组数字会立刻报警。其中「记忆召回不能因为改口径而掉档」是关键一条。

#### Changed

- **`tools/test_memory_recall.py` 改为使用全新会话**：它自己不密闭 —— 借用当前会话时，
  思维流残留的画像（`logs/mind_stream/<会话id>.json` 的 `了解`）会**压过**检索注入的记忆，
  使用率假红（实测换新会话后 命中 5/5、使用 5/5）。

#### Known Issues（**已发现、本轮未修**）

- `core/mind_stream/update.py` 的事实抽取会把**疑问句**当成陈述记下来：
  「我叫什么名字来着」→ 画像里存成 `名字：什么名字来着`；「我现在住在哪个城市」→ `城市：住在哪个城市`。
  这是**假记忆**，且画像比检索记忆更强势，会直接顶掉正确答案。属于下一轮修复项。

---

### 思维流：短路轮次也必须收尾

**🧠 缺陷：载体直答的那几轮（工具清单 / 反问 / 取回工具原文 / 超长输入改写）不写思维流状态，状态"时有时无"。**

#### Fixed

- **新增 `mind_done()`**（`xiaojiao_app.py`）：四处短路 `return` 之前先把本轮状态落盘。
  不落盘 = 这一轮对思维流不存在 —— 下一轮读到的还是上一轮的旧状态，`turn_count` 也不涨。
  验收实测：先说「帮我看看有哪些工具」（走短路、没存盘），紧接着「我要全部的」就被记成「（还没定）」，话题断线。
- **思维流改为喂融合后的请求**（`user_input_ctx` 而非 `user_input`）：思维流要记的是**用户想说什么**，
  不是**用户嘴上是哪几个字**。回指句「我要全部的」本身看不出话题，融合后才是「我要全部的工具」。

#### Verified

- `tools/test_mind_stream_live.py` **23/23**（7 条验收全通过）；`tools/test_mind_stream.py` 60/60。

---

### 架构文档补全：设计哲学后半篇（第十四至二十二节）

**📚 前十三节讲"小焦现在是什么"，这一篇讲"它接下来靠什么继续变强"。**

#### Added

- **`docs/design-philosophy.md` 新增八节**：精度叠加 / 速度优化 / 自我改进 /
  全局工作空间 / 小脑定位 / 意图理解交给模型 / 并发与状态一致性 / 可观测性。
  **每一节都标实现状态**（已落地 / 部分落地 / 设计未落地）并指向真实代码路径 ——
  不标状态的架构文档等于把"想做"当成"做过"。
- **`docs/architecture-diagrams.md` 新增 8 张 Mermaid 图**（图 12–19，与新增八节一一对应），
  配色统一、节点名中文、每图一句话说明 + 代码位置索引。全仓库 **76 张图，`check_mermaid.py --all` 0 问题**。
- **`README.md` 顶部补一条定位**：载体给模型叠加"等效精度"，4B ≥ 360B（存量精度 vs 流量精度）。

#### Docs

- 「小脑定位」一节把本次修的长文本失效**完整写进设计文档**：两个根因、三处改法、实测前后对照表、
  以及"为什么 500 字差 1 字做不到 < 0.99"的数学上限推导（固定维度池化把 N 个位置压成一个向量，
  改 1 个字最多让结果偏离 `2/√N`，N=500 时 cos ≈ 0.996 已是理论上限）。

---

### 四层修复：工具选择（调错工具 / 乱试 / 漏调）

**🧭 用户实测：小焦有时会调错工具、在工具海里乱试、或者该调不调。按四层从根上治。**

#### Changed

- **第 1 层 · 工具描述场景化**：所有可调用工具（`.py` / `.js` / `.json` 插件 + 内置）的描述
  统一改成「**什么时候用** + 输入 + 输出」，同类工具写明区别：
  `get`（静态页首选）→ `fetch`（要渲染）→ `stealthy_fetch`（被风控才用）；
  `net_ip`（查自己）vs `get_ip_info`（查指定 IP）；`search_files`（找文件名）vs `grep_files`（找内容）。
- **修掉重名工具（"调错工具"的根因之一）**：`read_file` / `search_files` / `search_content`
  在**内置、code_intelligence、workspace_search** 三处重名 —— 后加载的会覆盖路由表，
  模型以为调 A 实际执行 B。现在：`ci_*` / `ws_*` 改名区分，并且 `_build_tools()` 加**冲突防呆**
  （内置优先，重名跳过并告警）。
- **第 2 层 · 工具选择决策树**：`_TOOL_RULES` 增加【工具选择顺序】八条
  （网址→get→fetch→stealthy_fetch ｜ 信息→web_search ｜ 画图→archify 工作流 ｜
  漏洞→collect_vulnerabilities ｜ IP→net_ip ｜ 纯聊天→不调工具 ｜ 命令/文件 ｜ 兜底 web_search）。
- **第 3 层 · 入口关键词路由**（规则先于模型）：句子里有网址 → 直接走抓取；
  画图词 → 锁 archify 链；漏洞 → `collect_vulnerabilities`；IP/公网/归属地 → `net_ip`。
- **第 4 层 · 调错后的修正**：新增候选表 `_TOOL_FALLBACK`（`get`→`fetch`→`stealthy_fetch` …）、
  `_scrape_failed()` 判定（403/风控页/空正文）、**抓取自动升级**、
  `_layer4_after_call()`：同一工具**连续 2 次调错即停止**并如实报错（附建议候选）。
- **按意图收窄本轮工具**：画图轮只暴露 `archify_*`（+读文件/打开），网址轮只暴露抓取类 ——
  实测修掉了"画架构图跑去 read_memory，最后把记忆当答案"这种乱试。
- **多步工作流加时间预算**：画图轮 240 秒上限，超了停止并如实汇报进度（原来能跑到 400+ 秒）。

#### 实测（8 条验收用例）

| 用例 | 结果 |
| --- | --- |
| 抓一下 https://example.com | ✅ `get`，9 秒 |
| 帮我看看 www.gutenberg.org 写了啥 | ✅ `get` + 真实解读 |
| 抓 https://www.cloudflare.com | ✅ `get` 被拦 → **自动升级 stealthy_fetch** 成功 |
| 画一张小焦架构图 | ✅ 全链 read_skill→guide→read_schema→read_example→validate→**deliver**→visual_check，**47 秒**交付 HTML |
| 最近漏洞 | ✅ `collect_vulnerabilities` |
| 我的 IP 是多少 | ✅ `net_ip`，1 秒 |
| 你好 | ✅ 0 工具调用 |
| 今天天气怎么样 | ✅ 用了 `get_weather`（比硬搜更准） |

> 全量测试 **248/249 · 100%**（离线 92 ｜ 应用逻辑 104 ｜ 安全 18 ｜ 联网 34/35，1 项按当天 NVD 数据跳过）；
> 实机 36/36 ｜ UI 25/25 ｜ 预设 13/13 ｜ 文档 0 错误 ｜ Mermaid 42 图 0 问题 ｜ 原理 12/12 ｜ ruff 全过。

### 附：v1.1.0 阶段修复（并入本版）

### 校验熔断：archify validate 不再来回改 7 次

**⚡ 用户实测：让 Archify 画一张架构图，`archify_validate` 被反复调用 7 次、总耗时 287 秒 ——
根因是"模型每次只看到一两条报错，改一条再校验一次"。**

#### Fixed

- **校验失败一次列全所有报错**（`plugins/archify.py`）：
  - `_fmt_validate()`：每项检查的 details **全部**输出（原来只给前 2 条）、
    composition issues 不再截断到 10 条，并附「建议修法」映射表（按问题类型给具体改法）；
  - **`_fmt_validate_error()`（新增）**：node 校验失败时退出码非 0，原来直接把整块转义 JSON
    甩回给模型（几十条挤成一行）→ 现在拆成「字段路径 + 报错 + 建议修法」逐条清单，
    并明确写「一次改完，再校验一次；不要只改一条就重试」。
  - 结果：同一指令 **287 秒 → 39 秒**，`archify_validate` 真实调用 **7 次 → 3 次**。
- **同一工具连续失败 3 次熔断**（`xiaojiao_app.py` 工具循环）：
  `_tool_failed()` 判失败（只认明确失败信号，避免把"失败重试机制"这类正文误判）、
  `_tool_breaker()` 计数与熔断；触发时**停止重试**、工具轨迹标注
  「已熔断：连续 N 次失败，停止重试」，并把最后一次报错原文摆给用户。
  成功一次即清零，换工具重新计数。
- **提示词加规则**（`_TOOL_RULES` ④）：校验类工具失败必须按返回的**全部**报错一起修，
  禁止逐条试错；并告知会被熔断。
- **校验失败落盘**：`logs/archify_validate.log` 记录时间、图表类型、spec 大小与失败摘要
  （便于复盘"到底卡在哪几条"）。

### 抓取类指令改为规则直通（同一句话每次结果一致）

- **`_scrape_direct()`（新增）**：「抓一下 <url>」这类明确指令现在**在模型之前**先由规则判定并
  真调抓取工具（模型兜底路径保留）。实测原来交给云端模型时，它有时挑 `fetch`、有时挑
  `fetch_url` 连调五六次且不给代码块 —— 同一句话结果不一致，JSON 展示因此时好时坏。
  现在实测连续 3 次 `live_check` **36/36 稳定通过**。

### 附：早期修复（并入本版）

### 系统提示词分层重构（role 只留人设，规则与清单交给代码）

**🏗️ 架构缺陷修复：以前把「检索铁律」「Archify 工作流」「工具清单」都塞进 `role` 字段 ——
用户改人设就丢规则、加一个插件还得手改人设、role 越写越长把人设淹没。**

#### Changed

- **`compose_system_prompt(role, plugins=None)` 固定四段拼接**（唯一合成入口）：
  `role（纯人设） + _SEARCH_RULES（检索铁律） + _TOOL_RULES（工具规则） + _plugin_list()（插件清单）`。
- **`_plugin_list()` 从 `PLUGINS` 动态生成工具清单**（工具名 + 一句话描述，去重、超量折叠）；
  在 `load_plugins()` 末尾调用 `refresh_system_prompt(plugins)` 重建提示词 ——
  往 `plugins/` 丢一个新 `.py`、重启即生效，**不需要**改 role。
  （坑：`PLUGINS = load_plugins()` 的赋值发生在函数返回之后，函数内必须把刚加载好的表显式传进去，
  否则清单会生成成"当前无可用工具"。）
- **`strip_search_rules()` 升级为"剥掉所有规则类片段"**：`[检索铁律] [工具铁律] [简洁原则] [代码工作流]
  [技能插件] 【当前已加载的工具】 [环境] [工具用法]` 全部切掉，只留纯人设（幂等，兼容界面回传的合成人设）。
- **Archify 工作流从 role 搬进 `plugins/archify.skill.md`**（技能插件随插件走）；
  用户的 role 由 743 字精简为纯人设一句（原文件已备份）。
- 运行时不再重复拼 `_TOOL_RULES`（已在 SYSTEM_PROMPT 里），提示词只涨清单那一份。

#### Fixed

- **用户说"别搜/停止搜索/直接用某个工具"时不再联网搜**（`_search_forbidden`）：
  实测用户下命令"停止搜索。不要搜「用」字。请直接调用 archify_doctor 工具"，
  小焦却把"停止"当检索词又搜了一遍。
- **点名了已加载工具就不再搜**（`_named_tools`）：实测"用 Archify 画架构图"跑去搜「用」字，
  搜回"用（汉语文字）_百度百科"。
- **点名零参数/全可选参数工具时直接调用**（`_noarg_named_tool`）：`archify_doctor` 实测 1 秒返回真实环境报告。
- **多步工作流不再被工具轮数截断**（`_workflow_needs_more_rounds`）：画图类上限 6 → 14 轮，
  实测 Archify 全流程 18 次工具调用、287 秒跑完并交付 HTML（以前会停在
  "✅ 已完成「archify_read_example」：{…}" 这种中间产物上）。
- **Archify 输出文件名允许中文**：老规则只认 `[A-Za-z0-9_-.]+`，模型自然写"小焦系统架构"就直接报
  "非法文件名"交付失败；现在按路径安全判（basename + 禁 `..`/分隔符 + 结果必须落在输出目录内）。

### 本版核心修复（用户实测的两个真缺陷 + 自查隐患）

**🛡️ 修掉用户实测的两个真缺陷：漏洞查询不走时间窗（拿到 1999 年数据、受影响软件全 `n/a`、5 条只总结 1 条）+ 把功能字「用」当检索关键词（搜出"用（汉语汉字）"）。**

#### Added

- 🆕 **插件工具 `collect_vulnerabilities(days=7, severity="HIGH", limit=5)`**（`plugins/scrapling_bridge.py`，工具数 17 → 18）：
  漏洞查询不再让模型自己拼接口，**「拼 URL + 挑字段 + 排版」全部收进插件层**。
  - **强制时间窗**：`lastModStartDate` / `lastModEndDate`（UTC，`days` 默认 7、上限 120 = NVD 官方限制）；
  - **取数**：单页 50 条；窗口内记录多于一页时取「最新一页 + 最早一页」，保证最新几条一定在手里；
  - **等级**：CVSS 取值优先级 v4.0 → v3.1 → v3.0 → v2，v2 无 `baseSeverity` 时按官方分段补等级；
    `severity=HIGH` 表示 **HIGH 及以上**（含 CRITICAL），也支持 `HIGH,CRITICAL` 精确集合与 `ANY`；
  - **受影响软件**：从 CPE 还原人话（`cpe:2.3:a:apache:http_server:1.0` → `Apache HTTP Server 1.0`）；
    新 CVE 尚未收录 CPE 时，从英文描述里**保守摘取**并标注「（描述推断）」，摘不到就写「（NVD 未收录产品配置）」——
    **表格里不会再出现 `n/a`**；
  - **直接返回 Markdown 表格**：`序号 / CVE 编号 / 等级 / 评分 / 受影响软件 / 发布时间 / 摘要`，
    表头带时间窗、数据源、**实际扫描范围**、命中条数；**抽样不完整 / 无 CVSS 评分都会如实标注**，不把不完整讲成完整。
- 🧩 **主程序新增检索词清洗闸门**（`xiaojiao_app.py`）：`extract_search_keywords()` / `resolve_search_query()` /
  `detect_vulnerability_query()` —— 命令式口语（"用联网搜一下最近的漏洞"）会被清洗成真关键词并补 `CVE`
  （→「最近的漏洞 CVE」）；只有功能字/语气词时**直接反问答不出关键词**，绝不用单字去搜。
- ✅ **新增测试**：应用逻辑套件 `tests/stress/test_app_logic.py`（36 项：检索词清洗 / 漏洞意图 / 提示词铁律 /
  工具注册 / 配置热重载不丢铁律 / 切人设接口回归）、漏洞聚合离线 24 项 + 联网 8 项、
  实机 `live_check.py` 新增两个用户实测缺陷的回归用例（第 9、10 节）、
  `ui_check.py` 新增 `--vuln` 场景（真浏览器里数 `<table>`）。

#### Fixed

- 🔴 **漏洞查询不走时间窗**：模型自己拼的是 `…/cves/2.0?resultsPerPage=5&cvssV3Severity=HIGH`（**没有日期过滤**）→
  拿回 1999 年的历史数据；原始 JSON 丢给模型 → 5 条只总结了 1 条；受影响软件要模型自己从
  `configurations[].nodes[].cpeMatch[].criteria` 推 → 全部 `n/a`。现在漏洞/CVE/高危类问题**优先走
  `collect_vulnerabilities`**，实测「抓取最近 7 天的高危漏洞」→ 时间窗 `2026-09-05 → 2026-09-12`、
  7 行表格（表头+分隔+5 行）、等级全为 HIGH/CRITICAL、**5 行都带软件名**（CPE 还原 + 描述推断兜底）、耗时 2.8~3.6 秒。
- 🔴 **把功能字「用」当检索词**：用户说「用搜索工具找漏洞」，模型把「用」/整句当 query 丢给 `web_search`，
  搜回来的是"用（汉语汉字）"百科词条。现在 `web_search` 入口**强制清洗**（命令式删动作词、裸关键词保守不动，
  避免"看雪安全"被误伤成"雪安全"），清洗后无内容则返回中文提示「请告诉我你要搜索的具体关键词」；
  提示词里同时写明**检索铁律**（写进代码，换人设也不会丢）。
- 🐞 **潜在缺陷：漏洞聚合取原始响应用错了工具名**：`_fetch_raw` 里用了小焦的别名 `get`，而 inproc 路径按
  Scrapling **原生工具名**白名单分发 → 报「不支持的抓取动作：get」。改为原生名 `make_request`。
- 🐞 **潜在缺陷：统一日志的脱敏过滤器会打断所有 `%d` 型日志**：`_ScrubFilter` 原来把 `record.args` 每个参数
  `str()` 化，于是 `logger.warning("…连续失败 %d 次…")` 在 emit 时抛 `TypeError: %d format: a real number is required`，
  日志变成「--- Logging error ---」堆栈（**熔断告警就是这么被打掉的**）。现在改为「先把消息渲染成最终文本、再脱敏」，
  既不破坏格式化，也保证脱敏的是最终要写出去的那串字。
- 🐞 **结构化接口不再走 Markdown 转换**：9MB 的 NVD 响应经 Scrapling 的 extraction 后会被加上 `\_` 转义、
  长文本甚至被改写成**非法 JSON**（`json.loads` 直接失败，已复现）。现在 `_fetch_raw` 走原始 HTTP
  （SSRF / robots / 同域限速一步不少），并对 NVD 429 退避重试一次。
- 🐞 **用 ruff 的真 bug 级规则（`--select E9,F63,F7,F82`）又扫出 5 处潜在崩溃，全部修掉**：
  `brain_manager._llama_cfg()` / `_comfy_dir()` **根本没有定义**（`_start_llama` 一进来就 NameError，
  被 `except: return False` 吞掉 → **多脑"唤醒"永远静默失败**、切大脑形同虚设）；
  `/api/persona` 引用未定义的 `_CFG`（`_CFG` 只是 `_load_control()` 的局部变量）→ **界面切人设必然 500**；
  `api_voice_warm` 缺 `global _asr_model/_tts_model` → 模型加载完就丢进局部变量被回收（**预热白做**）；
  插件生成失败时的兜底模板 `TPL` 未定义 → 兜底路径 500（现在有了真正的 `_PLUGIN_TPL` 模板）；
  `ScraplingBridge._pending` 注解引用了未 import 的 `queue`（顺手把 `__import__("queue")` 改成正常导入）。
- 🐞 **99 处日志格式参数不匹配**（`tools/fix_silent_except.py` 注入的 `忽略异常(%s:行号): %s` 多传了一个行号参数）
  → 每次 emit 都抛 `TypeError: not all arguments converted...`，日志变成「--- Logging error ---」堆栈。
  统一改为 `忽略异常(%s:%d): %s`（17 个文件、99 处），并修正注入工具本身，避免以后再犯。
- 🔴 **CI 其实一直是红的（本轮查出并修掉）**：GitHub Windows runner 的控制台编码不是 UTF-8，
  `tools/check_mermaid.py` 打印中文时抛 `UnicodeEncodeError: 'charmap' codec can't encode character '\uff1a'`，
  于是**每一次** CI 运行都卡在"校验 Mermaid 原理图"这一步，后面的压力测试**从来没在 CI 里跑过**
  （查了近 10 次运行，最早可查到的那次就已经失败，全部同一个原因）。
  修法：所有入口脚本（`tools/check_mermaid.py`、`tools/audit_static.py`、`tools/check_docs.py`、
  `tests/stress/harness.py`）在启动时把标准输出/错误重新配置成 UTF-8（失败则退化为替换字符，绝不抛错），
  并在 workflow 里给 job 加 `PYTHONUTF8=1` / `PYTHONIOENCODING=utf-8` 双保险。
  本地已用 `PYTHONIOENCODING=cp1252` 复现原故障并验证修复后通过。
- 🐞 **配置热重载会丢掉检索铁律**：`reload_control()` 原来直接 `SYSTEM_PROMPT = role`，
  现在统一走 `compose_system_prompt()`（人设 + 铁律），并且 `/api/persona` 落盘前会去掉界面回传的铁律，避免重复叠加。

#### Changed

- 🔎 **实机验收新增两节**：第 9 节验证「用搜索工具找漏洞」不再把「用」当主题、第 10 节验证漏洞表的时间窗/行数/等级/软件名；
  实机验收 **29/29 通过**。
- 📚 文档同步：`docs/scrapling.md`（新增 2.5 节讲清漏洞聚合设计 + 验收清单 11/12 条 + 排错两条）、
  `README.md` / `ARCHITECTURE.md` / `docs/architecture.md` / `docs/testing-report.md` / `docs/landing-report.md`
  工具数与测试数据一并更新；按你的要求**删除 `docs/acceptance-report.md`**（验收内容改为在对话里直接给）。

#### Verified

- 全量压力套件 **182/182 通过 · 通过率 100% · 93.7s**（离线单元 + 应用逻辑 + 安全 18 + 联网，含真实 NVD 接口用例）；
- 实机验收 **29/29 通过**（真服务、真 HTTP、真 NVD）；
- 真浏览器渲染检查：漏洞表格渲染成真 HTML `<table>`，**控制台 0 错误**；
- **CI 首次全绿**（run 34691282396）：Mermaid / 静态审计 / 文档一致性 / ruff / 压力测试五道闸门全过，
  CI 内实测 **136/137 通过 · 通过率 100% · 74.9s**（1 条跳过：CI 未安装 flask/torch，应用逻辑套件按设计跳过）；
- Mermaid 原理图 42 张 **0 问题**；静态审计：已跟踪文件里**静默吞异常 0 / 裸 except 0 / 明文密钥 0**；
- `ruff --select E9,F63,F7,F82`（真 bug 级规则）**All checks passed**（整改前有 5 处未定义名）。

### Web UI 与检索体验（深色模式 / 代码高亮 / 表格排版 / 思维标签）

**🎨 用户实测反馈：深色模式下代码块像有"白色背景印记"，代码没颜色，漏洞表格太挤，回答里冒出 `<think>`；
联网搜索还会搜出"你（汉语文字）_百度百科"这类词典词条。**

#### Fixed

- 🔴 **深色模式代码块的"白色印记"（根因不是阴影，是背景叠了两层）**：`.b code{background:#2a3140}`
  会给**代码块内的 `code` 元素一并上色**，于是整块代码躺在一个浅灰方块上（比 `pre` 的 `#0d1117` 亮一档），
  看起来就像"文字背后有白色底纹"。现在 `pre.code code` 的背景 / 内边距 / 圆角 / text-shadow 全部显式清零，
  只有正文里的**行内** `code` 才保留浅底。
- 🔴 **联网搜索搜出词典词条（真根因在引擎侧）**：中文搜索引擎对"最近 X""2026年X"这种**前缀**
  基本只认第一个词 —— 搜「最近 AI 新闻」「最近的漏洞 CVE」返回的全是歌曲《最近》/词典词条。
  修法：① 检索词清洗再加固（"帮我搜索一下 你好" 不再被拆成"好"、寒暄词直接拒绝联网）；
  ② `web_search` 改为**多变体 + 相关度排序**：原词之后紧跟"只用主题词"的写法，按**主题词覆盖率**
  （≥60%）判断这批结果是否跑题，跑题就换写法；词典/翻译类词条降权。实测同一批查询从"全是《最近》"
  变成"全是 AI 新闻/漏洞情报"。
- 🐞 **回答里冒出 `<think></think>`**：模型偶尔把空的思维标签吐进正文。现在后端在落地回答前、
  前端在渲染前都会剥离（成对整块删 + 残留标签删 + 清多余空行）；
  顺带修掉一个判断写窄的小 bug：`</think>` 单独出现时不含子串 `<think`，早期版本会漏删。

#### Changed

- 🎨 **代码块终于有色了**：新增一套轻量语法高亮（Python / JS / JSON / Bash / SQL / CSS / HTML + 通用兜底），
  关键字 / 字符串 / 数字 / 注释 / 函数名 / JSON 键各自配色（实测一个代码块 51 个着色片段、4 种颜色），
  复制按钮与语言标签保持不变；超大文本（>40000 字）自动跳过高亮，避免卡顿。
- 📐 **表格不再挤成一团**：单元格留白 6px→**11×15px**、行高 1.65、外框 + 圆角 + 斑马纹；
  只有最后一列（摘要）允许折行，其余列 `nowrap`——不会再出现 `HIG H`、`CVE-2026-` 这种断裂；
  宽表横向滚动而不压缩字宽；**带表格的消息自动占满整栏**（实测气泡 1140px / 容器 1190px）。
- 📝 **文档措辞统一**：全项目文档改为客观陈述句式。
- 🔢 **版本收敛**：对外只保留 **v1.0** 一个版本（旧的 Release/tag 已清理），仓库内版本号与文档同步。

#### Verified

- 新增 `tests/stress/ui_style_check.py`（真浏览器量计算样式）：**14/14 通过** ——
  代码块底色 `rgb(13,17,23)`、`code` 元素背景 `rgba(0,0,0,0)`、`text-shadow: none`、
  高亮片段 ≥3、表格留白 15px、短列 `nowrap`、气泡宽度 ≥85% 容器、控制台 0 错误。

### 展示与体验：markdown 围栏、代码高亮、表格排版

**📄 完整落地报告 + Web UI 可读性优化。**

#### Added

- 📄 **`docs/landing-report.md`（完整落地报告）**：架构实况图、**功能全清单**（对话与模型 / 工具与插件 /
  记忆与自进化 / 多模态 / 桌面与工作区 / 运维可观测）、**Web UI 设计审查**（对照业界可用性结论，
  含已修正 7 项 + 优点 5 条 + 按性价比排序的优化建议）、测试与安全数据、文档清单、已知限制、健康度评分 8.8/10、后续路线图。

#### Changed

- 🎨 **` ```markdown ` 围栏渲染成真正的 Markdown**：模型常把表格用 ` ```markdown ` 包起来，
  旧逻辑当代码块显示（表格挤在灰色框里）。现在表格/标题/列表会**渲染成 HTML**（实测生成真 `<table>`：
  2 表 / 4 行 / 6 个表头），左侧保留细线标识"这块来自围栏"；其它语言仍按代码块显示并保留「⧉ 复制」。

### 抓取大 JSON 后的展示修复

**🔍 用户实测反馈的展示问题（真实截图投诉）：抓取接口后内容仍是一大坨原始 JSON。**

#### Fixed

- 🔴 **大 JSON 抓取后展示混乱（根因：截断顺序错了）**：`stealthy_fetch` 抓 NVD 接口返回 **10023 字**，
  正好越过 `MAX_CONTENT_CHARS=10000` 的截断线 —— 旧流程是「**先截断、后美化**」，
  内容被截成**非法 JSON**，于是美化与代码块双双失效，用户看到的是一整屏压缩 JSON。
  现在改为「**先美化、后截断**」，并且由调用方决定是否截断（`clip=False` 保留全文供展示与落盘）。
  实测同一请求：**10023 字原始 JSON → 3538 字 / 115 行美化 JSON + 折叠提示**，前端渲染为可复制代码块。
  > 这个 bug 之前没被发现，是因为我的验证用例用的是 6828 字的响应（**没越过截断线**）—— 现在补了
  > 9 项**超长 JSON 回归用例**（含 16KB 样本），CI 每次都会跑。
- 🐞 **抓到的接口存成 `.json` 却解析不了**：落盘内容带着 Markdown 转义（`NVD\_CVE`），
  `json.load` 报 `Invalid \escape`。现在存 `.json` 时自动「去转义 → 解析 → 美化」成**合法完整 JSON**
  （实测 24404 字、5 条漏洞、可被程序直接解析）；非 `.json` 文件名不改写内容。
- 🐞 **`save_to` 之前会存下"折叠后的预览"**：现在落盘始终是**全文**（实测 13033 字 vs 展示 3538 字）。

#### Verified

- 全量测试套件：**103/103 通过 · 通过率 100%**（新增 9 项大 JSON 回归用例）
- 端到端复测（用户原话）：模型路径 ✓ 直接产出 Markdown 表格；规则快通道 ✓ 产出可复制代码块 + 折叠提示

### 界面小修（模型下拉标签 · favicon）

**🔍 实机验收发现的两处体验修复**（UI 层，无接口变更）。

#### Fixed

- 🐞 **模型下拉误导性显示"未配置模型"**：`/api/models` 在未显式配置模型时返回空列表，
  界面于是显示"未配置模型" —— 但此时**自动模式下的本地大脑（:9292）其实是通的、对话也正常**，
  用户会以为没配好。现在改为显示「**自动（本地大脑 :9292）**」；只有既无模型又非自动模式才提示去设置页添加。
- 🐞 **浏览器控制台一直报 `favicon.ico 404`**：新增内联 SVG favicon 路由（不额外增加文件），控制台错误清零。

#### Verified

- 实机验收 **23/23 通过**（对运行中的服务发真实请求：8 个端点、体检 11 项、抓取、JSON 展示与折叠、SSRF 拦截、指标累加、会话回收器、日志脱敏）
- **真浏览器（Playwright）打开 UI**：发送消息 → 抓取回答渲染正常（代码块/链接/标题/📖解读）→ 控制台 **0 错误**，已留截图证据
- 全量测试套件：**94/94 通过 · 100%**

### 稳定化落地（指标 / 统一日志 / 会话回收 / 批量并发 / CI）

**🛠️ 稳定化落地版**：可观测（指标 / 日志）、可回收（会话）、可并发（批量）、可验证（压力测试 + CI），
并把文档补成「能照着做」的形态。**语义化版本说明**：全部为兼容性新增与内部整改，**无破坏性变更**。

#### Added

- 🛡️ **安全审计报告 `docs/security-audit.md`**（新增）：SSRF（直连 + 重定向 + **数值型绕过**）/ robots /
  限速 / 目录穿越 / 日志脱敏 / UA / 命令端点 / 无遥测 / 无明文密钥 的逐项结论与复现命令；
  附带安全控制点流程图。**本轮审计发现并修复 2 个真实问题**（见 Fixed）。
- 🧪 **测试与稳定性报告 `docs/testing-report.md`**（新增）：测试资产清单、执行结果、**覆盖矩阵**、
  4 项未覆盖清单、稳定性压测方法与判定规则。
- ⏱️ **稳定性压测脚本 `tests/stress/stability_30m.py`**（新增）：30 分钟无头压测 —— 高频抓取 / 连开会话不关闭 /
  必失败 URL 打熔断退避，每 30 秒采一次内存与会话；**直接调用插件的 `execute()`，不经过大脑、不消耗任何大模型
  Token**；自动判定 PASS / SUSPECT / FAIL 并产出 `logs/stability_30m.md` + `logs/stability_30m.json`。
- 🔐 **安全用例套件 `tests/stress/test_security.py`**（新增，18 项，已接入 `run_all.py`）：SSRF 21 种写法矩阵、
  robots RFC 9309 三种分支、同域限速计时、日志脱敏**回读文件验证**、UA 合规、目录穿越、命令端点源码契约、
  无遥测埋点、已跟踪文件无明文密钥。
- 📐 **架构说明 `ARCHITECTURE.md`**（新增）：设计目标 → 系统总览图 → 进程与端口 → 模块职责表 →
  **一次对话的完整生命周期（时序图）** → 插件机制与契约 → 大脑/小脑协作 → 数据与状态 →
  质量与安全 → **扩展点（改哪里）** → 已知限制；含 5 张 Mermaid 原理图。
- 🤝 **贡献指南 `CONTRIBUTING.md`**（新增）：五分钟上手、分支/提交规范（**中文 commit**）、
  提交前自检、**8 条硬性约束**（禁硬编码/禁明文密钥/错误必须中文/不许静默吞异常…）、插件模板、报 Bug 规范。
- 🧭 **图语法自检 `tools/check_mermaid.py`**（新增）：离线校验所有文档里的 Mermaid（围栏闭合、
  subgraph/end 配对、引号与括号配对、sequenceDiagram 关键字），已接入 CI 作为**文档质量闸门**；
  当前全仓 **39 个图 0 问题**。
- 🖼️ **前端渲染自检**（补进 `tests/stress/test_units.py`）：真测后端转换（Setext→ATX、JSON 归拢）
  + 对渲染模板做**契约检查**（`_fence_body`、`codebox lang-`、`renderTableBlock`、`mdh`、`srcbox`、`<br>`、`/metrics`）。
- 🪵 **统一日志模块 `xiaojiao_log.py`**：所有模块走同一套 logging（`logs/xiaojiao.log`，5MB×3 轮转），
  支持 `XIAOJIAO_LOG_LEVEL` / `XIAOJIAO_LOG_CONSOLE` 调节；**写日志前全链路脱敏**（`api_key=`、`sk-…`、`ghp_…`、`AKIA…`、JWT 一律打码），
  日志目录不可写时自动降级、绝不影响主流程。
- 🔧 **安全重构工具 `tools/fix_silent_except.py`**：用 AST 精确把「`except: pass`」改成「记日志 + 明确降级」，
  把裸 `except:` 改成 `except Exception:`；默认 dry-run、改写后自动语法校验、语法不过则放弃该文件。
- 🧪 **压力测试套件进仓库 + CI 定时跑**：新增 `tests/stress/`（`run_all.py` 编排 + `harness.py` 骨架 + 离线与联网两套用例，结果机读 `results.json`），以及 `.github/workflows/stress-test.yml`（每天 03:00 定时 / 手动触发 / 插件或测试变更触发；**通过率 < 95% 直接失败**；结果上传 artifact 并写入 Job Summary）。本地实测 **67/67 通过 · 通过率 100% · 74.3 秒**。用法见 [tests/stress/README.md](tests/stress/README.md)。
- ⚡ **批量并发可配置（BatchConfig）**：原来批量是串行的，3 个域名也要排队；现在拆成**跨域并发**（`batch.concurrency`，默认 3）与**同域闸门**（`batch.per_domain_limit`，默认 1，永不并发打同一个站），外加 `rate_limit` / `max_retries` / `backoff_base`。实测 3 个不同域名 **6.20s → 0.92s（提速 85%）**；同域实测最大并发仍为 1。非法配置（如 `concurrency=0`）**不静默忽略**，批量工具直接返回中文错误。复测 **18/18 通过**。
- 📊 **指标与观测（MetricsCollector）**：每次工具调用自动记录 `calls / success / fail / total_latency / avg_latency / max_latency / circuit_breaks / last_error`，三种取法：`GET /metrics`（Prometheus 文本，可直接抓取）、`GET /api/scrapling/metrics`（JSON，含活跃会话明细与熔断状态）、`logs/scrapling_metrics.json`（落盘）。安全拦截（SSRF/robots）不计失败；错误信息**脱敏后**入库。实测 **17/17 通过**，`/metrics` 线上返回 200。
- 🔒 **`sanitize()` 脱敏补强**：原来只认 `key=value` 形式，**裸凭据会原样泄露**（指标自测发现 `sk-xxx` 未被抹掉）。现在额外覆盖 `sk-…` / `ghp_…` / `AKIA…` / `xox…` / JWT / `password=`，日志与指标一律打码。
- 🧹 **会话自动回收（SessionManager）**：`open_session` 每开一次就真起一个浏览器，忘了 `close_session` 会一直占内存。现在三条规则任一命中即自动回收并真关闭：**TTL**（`session_ttl`，默认 30 分钟）/ **空闲**（`session_idle`，默认 5 分钟）/ **上限**（`max_sessions`，默认 20，超出踢最久未用 LRU）；后台线程每 60 秒巡检；配置非法（0/负数/非数字）回退默认并中文告警。实测 **10/10 通过**（LRU、TTL、空闲、真实会话回收、用户主动关闭从回收表移除）。

#### Changed

- 🧹 **消除"静默吞异常"（阶段 2 代码质量）**：静态审计发现 `except: pass` 共 **103 处**、裸 `except:` 19 处 ——
  出错时无声无息，线上完全无法排障。用 AST 工具批量改为「`LOG.debug("忽略异常(文件:行): 异常")`」，
  并给裸 `except:` 加上 `Exception` 限定（不再吞掉 `KeyboardInterrupt`）。
  审计结果：**静默吞异常 103 → 6、裸 except 19 → 14、疑似密钥 0**（剩余均在本地未入库的自用插件里）。
- 🧹 **清理死代码**：插件加载器里重复的 `elif man.get("tools")` 分支（永远不可达）已删除。
- 🧹 **行尾一致性**：仓库里 CRLF/LF 混存曾导致"整文件被改"的巨型 diff（`xiaojiao_app.py` 7232 行噪音，真实改动仅 73 行）——
  已按各文件在 HEAD 中的原始约定对齐并强制重新入库，现在 diff 干净可评审。

#### Fixed

- 🔴 **SSRF 数值型写法绕过（高危，安全自测当场发现）**：`http://2130706433/`（= `127.0.0.1` 的十进制写法）
  能绕过原有检查 —— 它既不以 `127.` 开头，本机 DNS 也解析不了（走进"解析失败→放行"分支），
  但 HTTP 客户端会把它当 IP 用，等于**直接打本机**。现在统一把十进制 / 十六进制 / 八进制 / 缩写点分
  （`2130706433`、`0x7f000001`、`017700000001`、`127.1`、`10.1`、`192.168.1`）还原成 IP 再判定，**6 种写法全部拦截**。
- 🟠 **命令执行服务默认暴露局域网（中高危，源码审计发现）**：`xiaojiao_tools.py` 的 `/api/run` 无鉴权、能执行任意
  PowerShell，却默认监听 `0.0.0.0:5003`，且 `force` 默认 `True`（跳过危险确认）。现在**默认只监听 127.0.0.1**
  （局域网使用需显式设 `XIAOJIAO_TOOLS_HOST`），且**只有本机客户端**才允许跳过确认（非本机请求降级 + 记警告）。

### 内置 Scrapling 抓取 + 小脑必需化 + 安装器分级

#### Added

- 🕷️ **内置 Scrapling**（`plugins/scrapling_bridge.py` 桥接插件）：小焦从此**想抓啥抓啥**——网页 / 动态页 / 接口 JSON / 批量列表 / 登录态页面 / 下载任意文件。**Scrapling 原生 13 个工具 1:1 全部暴露（工具名与官方一致）**，另加 3 个小焦增强（`get` 友好别名 / `scrape_with_selector` 自适应选择器 / `download` 任意文件下载），并保留 `browser_session` 聚合入口 → 对外共 **17 个工具**：
  - 原生 13：`make_request` / `bulk_get` / `fetch` / `bulk_fetch` / `stealthy_fetch` / `bulk_stealthy_fetch` / `open_session` / `open_request_session` / `close_session` / `list_sessions` / `session_fetch` / `session_make_request` / `screenshot`
  - 小焦增强 3：`get`（`make_request` 的中文友好别名）/ `scrape_with_selector`（自适应选择器，防站点改版）/ 🆕 `download`（**下载任意文件** PDF/EPUB/ZIP/图片/音视频…，Scrapling 原生没有这个能力）
  - 兼容入口 1：`browser_session`（用 `action` 一个工具走完 open/fetch/screenshot/close 全流程）
  - 🆕 `save_to` 参数：抓取正文直接存文件到 `books/`（长文/连载章节不塞对话）
  - 会话类型用错时给**中文提示**（如 dynamic 会话不能发 HTTP 请求，会告诉你去用 `session_fetch` 或先 `open_request_session`）
- 📖 **抓完自动解读**：`_explain_content()` 让大脑按「是什么 / 关键要点 / 怎么用」逐条讲；模型不可用时退回规则提纲。
- 🧠 **用户使用时学习**：`_learn_skill()` 每次用户使用工具后沉淀经验（成功=正确用法、失败=原因+反思）→ `self_learn/tool_skills.txt` + 向量库；`_recall_skills()` 下次检索命中即注入上下文复用（**越用越会**）。
- 🎯 **抓取意图直通**：`_detect_scrape_intent()` 用规则识别「抓/爬/下载 + 网址」→ 直接构造并执行工具调用（不指望 4B 模型自己选工具/避免它编造代码）。
- ⚙️ 依赖与配置：`requirements.txt` 增加 `scrapling[fetchers]` / `markdownify` / `mcp`；`xiaojiao_control.json` 增加 `scrapling` 配置段（mode/proxy_list/rate_limit/timeout/circuit_breaker…）。
- 📚 文档：新增 `docs/scrapling.md`（工具详解/原理/配置/测试清单/排错/合规边界）；README 增加 **🕷️ 内置 Scrapling · 想抓啥抓啥** 章节（含架构数据流图 + 学习闭环图，位于致谢之前）。
- 📚 文档：README 增加 **🛠️ 一键安装 · 检测分级** 章节（含三张图：**安装必需/可选分级图**、**小脑路径三级解析图**、**安装器改动全景图**＋原则落地对照表），同样位于**致谢之前**；`docs/architecture.md` 增加插件小节。

#### Changed

- 🧠 **小脑改为必需项**（项目核心）：一键安装会检测小脑并明确提示；**路径不写死**（环境变量 → 配置 `brain.xiaojiao.model_path` → 项目目录探测 → **全盘自动探测**，模型与词表可跨目录配对、体积优先），支持换任意自训模型当小脑。
- 🛠️ **一键安装检测分级**：必需（Python 依赖 / 聊天大脑 llama.cpp / 大脑模型 / llama-swap 秒级切换 / 小脑）与可选（ComfyUI 视频大脑 / 视频节点 / Wan 视频模型 / Node.js / 猫娘 / 配音 / 封面 / 音乐）分开报告。
  - ComfyUI、视频节点、Wan 视频模型、Node.js 由「必需」降级为「可选」，不再阻塞启动。
  - Wan 视频模型 2.5GB 下载改为**先询问**（可选）。
- 🐱 **猫娘改为询问式**：启动时问「是否启动 N.E.K.O. 猫娘」(`[Y/n]`)，**不启动不影响小焦**；非交互环境默认不启动；`XIAOJIAO_NEKO_AUTO=1` 可免询问。
- 🖥️ **抓取结果展示优化**：正文直显（不再被小模型"总结"吃掉）；工具轨迹压成一行摘要（不再刷原始 JSON）；Setext 标题自动转 ATX。

#### Fixed

- 🧪 **全工具极限压力自检修复批次**（17 个工具 × 正常/空参/非法/必失败/连续 + 8 项对抗 + 性能基准，共 4 轮**真实调用**）：
  - 🐞 **`download` 把 404 错误页当文件保存**：目标 404 时插件仍报"已下载（类型 text/html）"并把错误页存成 `.pdf`。现在只有 2xx 才落盘；404/410 直接报"文件不存在，未保存任何文件"；只有 401/403/406/429（WAF 拦 UA）才回退浏览器指纹通道；0 字节、扩展名与内容类型不符也会明确警告。
  - 🐞 **批量 `urls` 传字符串被逐字符拆开**：`"https://a.com"` 被当成 **14 个单字符"网址"**，静默返回 14 条无意义错误。新增 `_norm_urls()`：字符串当 1 个网址、空列表报"urls 为空"、其它类型明确报错。
  - 🐞 **批量"全部失败"却返回成功**：顶层 `error` 为空 + status 200 → 调用方与熔断器都当成功，坏源永不被熔断。现在全失败置顶层错误（保留 `items` 明细）。
  - 🐞 **非法 `session_type` 毒化会话表**：非法类型会让 Scrapling 注册一条脏会话，此后 `list_sessions` **每次**都抛 Pydantic 校验错误（会话列表永久不可用）。现在插件前置白名单校验（dynamic/stealthy/static），`list_sessions` 另加可读降级提示。
  - 🐞 **用户传的 `timeout` 被静默忽略**：`get`/`fetch`/`stealthy_fetch`/`scrape_with_selector`/批量只用了配置默认值 —— 实测"6 秒超时"等了 **22 秒**才回来（Scrapling 默认重试 3 次 × 6 秒）。现在显式 timeout 会带上参数、收敛 `retries=1`、并给客户端加硬上限（实测 get 7.2s / fetch 13.0s 内返回）。
  - 🐞 **`scrape_with_selector` 选择器没匹配到却"空内容 + 成功"**：现在返回结构化 `not_found`（含 `not_found: true` / `selector`）与中文建议，不会返回「成功但空内容」这种误导结果。
  - 🐞 **英文/裸库错误外泄**：`Session 'x' not found`、`validation error for SessionInfo`、`net::ERR_NAME_NOT_RESOLVED`、`curl: (28)`、`Redirect to internal IP ... rejected` 等一律转成可执行中文（新增 `_humanize_error()`）。
  - 🔒 **回归确认**：SSRF 直连（含 `file://` / `169.254.x` / `[::1]` / `0.0.0.0`）与**重定向型 SSRF**（公网 302 → `127.0.0.1`）100% 拦截；`download` 目录穿越（`../../`）不逃逸出项目目录并提示已改名。
  - 📊 **复测**：17/17 工具、**32/32 用例全部通过**；熔断第 4 次触发且 32 秒后自愈；连续 20 次调用 Python 堆净增 0.10MB。
- 🐞 **插件加载器**：`spec_from_file_location` 未注册 `sys.modules` → Python 3.13 下**任何使用 `@dataclass` 的插件都会静默加载失败**。已修复（加载失败时清理 `sys.modules`）。
- 🐞 **前端渲染**：`renderBlocks` 按空行分块后块间缺换行 → 段落粘连；`.mdh` 标题类无 CSS → 标题不显示样式；`inline()` 不识别 Markdown 链接。均已修复。
- 🐞 **插件参数适配**：`_adapt_args()` 对不支持 `timeout` 的工具（`open_session`/`close_session`/`list_sessions`）硬塞 timeout → 报错；改为按工具白名单过滤参数并区分超时单位（浏览器类=毫秒，HTTP 类=秒）。
- 🐞 **抓取内容**：`extraction_type="markdown"` 缺 `markdownify` 会整体失败 → 改为提前探测 + 优雅降级（html 提取 + 内置 HTML→Markdown）。
- 🐞 **下载被 WAF 拦（403）** → 下载改用标准浏览器头，失败自动退回 Scrapling 浏览器指纹下载。
- 🐞 **熔断误判**：安全拦截（SSRF/robots/参数缺失）曾被计入"工具失败"，连续拦截会导致工具被误熔断 → 安全拦截不再计入。
- 🐞 **批量工具参数错误**：批量内部曾把单个 `url` 传给 `bulk_*` 工具 → 改为逐 URL 调用单个工具（这样才能真正控制限速/退避/失败隔离）。
- 🐞 **视频任务残留报错**：前端对已结束的视频任务轮询报「任务状态丢失」并混进对话 → 改为静默清理 localStorage / 温和收尾。
- 🐞 **robots.txt 误拦整站（真实案例：NVD API 抓不了）**：`urllib.robotparser` 遇到 401/403 会设 `disallow_all=True`，于是 **WAF 把 robots.txt 请求 403 掉 → 整站被判"禁止抓取"**；而用浏览器 UA 去看那些站点往往**根本没有 robots.txt**（404）。改为插件自己拉取并按 **RFC 9309** 判定：只有真读到 `Disallow` 才拦，404/403/超时/5xx 一律放行；同时新增**单次放行开关** —— 对话里说「忽略 robots 抓一次」（`ignore_robots: true`）或配置 `scrapling.allow_robots_skip: true`，并给出可执行的中文提示。
- 🐞 **多进程占用 5000**：旧 python 进程未退出时与新进程同时监听，导致"重启后改动不生效"（旧进程抢答）→ 文档补充排错步骤。
- 🐞 **残留硬编码路径全部清除（"不准写死路径"）**：
  - 玩具体检 `/api/env`：ComfyUI / 视频模型 / llama-swap 改为 `_discover_paths()`（配置 → 环境变量 → 全盘自动探测），并**后台线程预热**（体检首屏 ~1 秒，不再卡盘）。
  - `video_service/config.py`：视频模型根目录改为从 `brain.comfy_dir` 逐级上溯找 `dit_fp8.safetensors` + 全盘探测（原来写死的旧目录已失效 → 现在无论装在哪都能找对）。
  - `podcast_service/podcast_gen.py`：配音模型目录改为项目目录/家目录/下载 + 盘符关键词探测。
  - `plugins/scrapling_bridge.py`：自备 Chrome 改为项目内 + 家目录 + 各盘关键词目录探测。
  - `start_xiaojiao.py`：猫娘（Steam 版按盘扫 `steamapps\common\n.e.k.o` + `discover_neko()` 兜底）、llama-swap 全部改为自动探测。
  - `.gitignore`：补 `books/`、`downloads/`、`test.db` 与个人本地插件，避免抓取产物/自用插件误入库。
---

### 初版能力（首次发布）

**🚀 首次正式发布**：N.E.K.O. 猫娘桌面伙伴 + 多大脑秒切 + 全新封面 + 社区规范。

#### Added
- 🐱 **N.E.K.O. 猫娘桌面伙伴**：一键启动自动拉起你下载的猫娘服务（main_server:48911 / memory_server:48912），并后台学习你与猫娘的对话。
- 🧠 **多大脑·秒级切换**：`brain_manager.py`（RUN/WARM/OFF）+ llama-swap(9292)。
- 🎨 **全新 README 封面**：用户猫娘壁纸（`assets/xiaojiao_cover.jpg`）。
- 🎬 **真·文生视频**、🎙️ **播客大脑**、🎵 **音乐**、👁️ **视觉识图**、💰 **成本看板**。
- 📚 社区文件：`CODE_OF_CONDUCT.md`、`.github/ISSUE_TEMPLATE`、`.github/PULL_REQUEST_TEMPLATE.md`。

#### Changed
- 模型表述改为**可插拔**（兼容任意 OpenAI 大模型），去掉写死的型号。
- 安装向导 `install_all.py`：模型检测改为**本地任意 GGUF 或 云端兼容 key 任一通过**，并补齐可选功能依赖检测（N.E.K.O./配音/封面/音乐）。

#### Removed
- 旧 Electron 桌面宠物（`desktop/`、`jarvis_desktop.py`、`docs/jarvis-desktop.md`），桌面形象统一改用 N.E.K.O. 猫娘。

#### Fixed
- 安装向导不再死锁 `xiaojiao1.0-4B` 型号。
- `/api/env` 模型检测改为模型无关（本地 GGUF 或云端 API 任一即算有）。
- 网页顶栏 "J.A.R.V.I.S." 改为 "🐱 猫娘"，`/pet` 重定向到 N.E.K.O. 页。

---

### 更早的演进快照（已并入本版）

- 桌面宠物 / 视觉接口 / 成本看板 / 插件万能桥
- 会话侧边栏 / 会话持久化
- 聊天历史持久化 / 插件接入工具系统
- 原生 function calling、多步任务自动执行
- 初版：完整壳 + 工具 + 记忆 + 会话 + 联网 + 插件

> 这些中间版本已全部并入当前发布的 **v1.0**。
