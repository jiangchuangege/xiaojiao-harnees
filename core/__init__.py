# -*- coding: utf-8 -*-
"""小焦 · 载体层（carrier layer）

「模型是工人，载体是工厂」—— 这个包放的是**工厂**的部分：
任务分解、上下文装配、外部存储、结果拼装、循环调度、工具调度、校验纠错。

模型只处理"当前这一小块"；所有"无限"都由这里实现，换哪个大模型都不影响。

模块一览（对应六个无限）：
  embedder.py    文本 → 512 维向量（记忆无限的地基）
  memory_vec.py  对话记忆的向量库（追加式 JSONL，外部存储）
  retriever.py   检索 + 时间衰减 + 注入用文本（记忆无限）
"""
